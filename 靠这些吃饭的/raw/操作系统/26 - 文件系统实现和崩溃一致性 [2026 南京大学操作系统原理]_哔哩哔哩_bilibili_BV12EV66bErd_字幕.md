上节课的时候我们讲了文件系统呃

什么是文件系统呢

文件系统就是一个给你提供目录树状的结构的任何的东西对吧

我们其实在课上面从最开始的时候就反复的用

比如说PROCFS

比如像cs f s这些

它其实是虚拟出来的文件系统

只要你实现了一个

比如说如果甚至你是在用户态实现这个fire system

User space fs operations

你只要实现像我能便利目录

能创建目录

删除目录

Get attributes

open read write这些呃

基本的文件系统的功能

包括目录数的结构和文件的读写哈

它就是文件系统

然后你也看到很多例子了对吧

我上次甚至做了一个gal game fs

我可以维护一个自己的呃文件系统内部的状态

所以虽然我说它是文件系统

但其实不是它是一个gal game

然后它可以在屏幕上显示出一个游戏呃

它可以通过向外暴露一个目录数

然后你可以读写那个目录数实现任何东西

所以理论上说你可以创建出任何的

只要是一个这样的符合我们UNIX反复使用的目录树结构的东西

它就是呃就是文件系统

然后我今天来讲文件系统的实现

也就是我们要在呃block device上呃

实现一个文件系统这样的数据结构

并且能够实现persistence持久化

还是我们呃操作系统很重要的呃主题

然后我们也知道mount这个系统在用对吧

如果你看mount的系统调用的话对吧

它要求一个block device

这个block device有三个就有三个接口

你可以这样理解啊

实际上它会呃更复杂一点

它有一个队列

然后你可以往那个队列里面提交请求

并且你能知道这个请求请求完成

但呃简单起见

你你可以对比类比我们并发程序

写程序的时候的共享内存也有点像啊对吧

内存也是一个字节序列

只是内存它的呃寻址方式是按字节寻址的

你可以在memory hierarchy帮你藏起来了

整个从l one l two l three

然后到memory呃

甚至还有更多层级的存储的存储站

内存站

然后你可以用load和store去按字节的方式去寻址

然后类似的我们的呃blocky vice也是它是它是一个BLOCKERY

一个数组

然后它数组可能是4K或者比如说软软盘上面是512个字节

你看着那个快去一起把它读出来

写进去

以及呃并发程序里面

我们有一个说说data race是很危险的对吧

如果两个呃线程同时读写一个memory location

那就很危险

所以我们可能要用互斥锁呃这样的东西来实现呃互斥

然后我们的block device也有这样的一个类似于barrier的指令

它可以像并发程序里面

你能够从并行变成串行一样呃

它有一个叫叫block sink

它可以等待呃ongoing in的所有正在在图当中的写落盘以后再返回

你可以你可以这样理解

就用这三个操作来实现呃

你可以就用它来实现任何数据结构

就像你可以用load和store来实现任何的内存里的数据结构一样

但因为当我打了并发的这个比方的时候

你们马上也有一个感觉

是我又再一次打开了魔鬼的盒子对吧

我说多处理器编程从入门到放弃的时候

我们打开了一个魔鬼的盒子

然后我今天会再打开一次

我会讲呃

文件系统怎么实现

这就相对来说实现其实比较简单的

但是要实现一个crash safe的文件系统

你会发现你会遇到和并发编程里面一样的呃

问题很有意思啊

好我刚才讲了一个overly simplified的这样的一个呃

快设备的存储设备的模型

它可以读一块写一块呃

可以等待写入的快落盘呃

但实际上的设备会比这个要稍微复杂一点

比如说啊如果你在终端里面执行这个list block

你们确实可以看到一些设备

包括包括block device

包括它的分区对吧

这是MMC是我的SD卡和block0的P1第一个分区

那第二个分区你们可能会看到一个像NAME的这样的一个呃分区

实际上一个block device

比如说一个磁盘SSD或者HED

它本质上也是一个数据结构

然后这个数据结构也是有specification的呃

像我们今天主要用的是叫GPT

不是那个ChatGPT的

GPT是叫GUI d partition table

叫GUID的分区表

它是UEFI的specification的一部分

由于EFI是那个firmware

就固件的specification

你会看到为什么是由固件的厂商来制定这个标准对吧

因为你的磁盘的数据结构的呃

layout在系统一启动的时候

former就要看到对吧

如果他没有办法解析这个分区

他就不知道

比如说操作系统到底装在哪

你的数据盘到底装在哪

它应该怎么样的挂载所有的呃

你可以启动磁盘都遵循这个这个标准

有一系列的工具可以去读它

可以去写它

比如说呃去创建这样的系统盘

比如说创建一个可以启动的U盘

如果你希望做一个可以启动的U盘的话

你就需要把它格式化成这样的一个呃GUI d partition table的分区

这是一个数据结构

然后像他告诉你哦

O头是什么

然后数据结构在哪里啊

怎么样识别这个文件系统是什么

然后以及呃sorry怎么识别这个分区嗯

有多大

然后以及每个分区所在的啊这个位置它就是一个数据结构

然后我们通过读整个磁盘或者说存储设备的前面那一部分字节

你就可以知道哦

第一个partition在哪

第二个partition在哪

所以如果说我是一个比如说是一个完全被我写

比如写了全零的一个存储设备

我插上来的话

它是识别不出来这种P1P2的

他只能看到一个

可能只能看到一个mc block0

比如说我现在打个比方

我可以干干这样一件事

zero对吧

然后我直接把它重定向到这个m i c block里

然后我这个机器马上就成砖了对吧

我就把它全刷成零了

这时候你就看不到P1P2

只能看到呃这个block好

这是实际设备的情况

它也可以是一个虚拟的设备

甚至我们可以一层一层叠很多层的虚拟设备

如果你想要挂载一个文件系统里面的文件

就像我前面给大家展示

我可以mount一个文件镜像对吧

点image

然后fat32

我可以把它挂载到我的呃文件系统的目录里

实际上LINUX系统里面有一整套虚拟化层

虚拟化层它叫LVM叫呃logical volume manager

既可以实现一对多

也可以实现多对一

也可以实现多对多

所以理论上说我可以干一件什么事呢

我可以创建好多image2点image

然后我可以通过move back device就回环设备去把它变成一个

比如说这个杠D为杠loop0对吧

就是杠D为杠loop1

我可以变成好多个呃回环设备

然后它每一个都是一个看起来就像是一个block device的东西

可以把它虚虚拟成一个这个volume group

呃然后我在这个上面又可以创建好多个logic bom

就是可以随便虚拟化

因为本质上所谓的快设备

它就是一个block的ray

原则上我甚至可以用多块磁盘来虚拟出一块更大的磁盘对吧

呃你你们如果读那个教科书的话

看REID的话

我们也可以用多块磁盘去虚拟出一块更快的并且更可靠的啊磁盘

虽然我今年的课时可能没办法讲那个瑞德了

你们你们课后可以去读一下那个o step的教科书

上面的讲瑞德那个章节也很精彩

既然啊既然你知道可以做这件事对吧

比如说我甚至可以创

然后在这个上面我可以创建一个虚拟磁盘

然后把这个虚拟磁盘我可以变成P1和P2对啊

比如说我我甚至可以P1比P1

我可以给一个fat p2

我可以给一个e s e four

就像我们在list block里面看到的那样

然后我所以我也做了这样的一个

就做了这样的一个脚本

而且呃完全是y web coding的

我几乎没有没有管中间的呃

过程我让AI帮我实现了两个呃脚本

一个叫l v m demo

一个叫l v m clean up

然后这个l v m demo的功能就是说我可以运行一下诶

他就干了很多的事

我们先看一下日志啊

他创建了迪斯K123456788个磁盘

每个都是32兆

接下来他又创建了好多个

Look at that device

就像就像这样回环的设备

这样我就有好多个快设备了

这些快设备就是一个一个的physical volume对吧

它看起来就是一个一个的一个磁盘

对

看起来就是一个block device

然后呃再创建一个volume group

就把他们虚拟成一个虚拟的磁盘

叫啊vg demo

然后vg demo创建了以后

你看现在一共有224兆

然后我再把它格式化成一个GUI d partition table

格式化以后就把它变成P1和P2

然后一个是fat

一个是e x t four

然后啊全部都全部都完成了

也就是说你们现在需要的是有概念里面

我用到了很多的工具

LVM的工具以前都是要哎呀查手查手查手册

先把概念弄清楚

然后自己执行那个命令

现在不需要了

现在你只要知道有这个概念

能这件事情是能做的

我现在就有一个叫P1

有一个东西叫P1

有个一个东西叫P2

两个目录就在对吧

虽然是root在在我在在我这里好

然后我在P1上面

我比如说我就可以hello到A点TXT

然后我可以

P2下的B点TXT

注意这俩这些东西都是虚拟的

最后他们都会走到回环设备的一点一妹纸

2.1妹纸这些文件上

所以你看我现在现在假设我做一个sink

或者我啊做一个sink就可以了啊

我我确保我确保这个系统已经落入磁盘当中

确保这个写入到文件里了

然后我可以干这样的一件事

你们觉得hello和world会在哪

刚才我写了两个文件对吧

我在我在P1里面写了一个hello

在ex呃

可能在P2里面我不记得是什么文件系统

写一个word

然后这在这在一个比如说A点TXT里

就在一个B点TXT里

然后你可以看到他们正确地被写到了路由

到了两个我的文件当中对吧

因为我是前一半和后一半的

我有一个GUI id partition table

然后前一半是一个文件系统是P1

后一半是另一个文件系统P2

然后你看到这些数据真的就在这个磁盘的呃

虚拟的文件上很有意思

然后我还特意让他把所有的s trace的log都打印出来了啊

这都是就真的是只要讲一句话就行了

我我就跟他说好

你你能不能帮我把每一个LVM相关的呃命令里面的system call给扒出来

然后这样我就可以啊对吧

我我已经准备好这个命令了

我可以打印这个s trace点log

然后我让AI帮我解释呃

这些用来管理LVM的呃system cost啊

那那你看我因为我是用一个pipe

我就把整个s trace的log就送给了大圆模型

然后让大圆模型稍微思考两秒钟啊

第一建立loop back device啊

这这是又是我前面上课的时候讲过的对吧

他去呃杠DV杠loop control的这个文件上面做I/O control对吧

用loop control get free去得到一个空的loop device

创建这个好多个loop back device

呃

主要用的是I/O control

然后再这个pv create的时候就初始化这个物理卷的时候

相当于呃LVM已经开始呃起作用了

你看他用的是杠低为下的mapper下的control一个映射器

也是你看操作系统里面就把所有的这些功能

LVM这些功能全部都丢给了啊

I o control

你看他找到了

确实找到了这个呃block device的大小对吧

然后呃各种写入标签等等

然后创建一个组

创建一个组好

创建逻辑卷

就和刚才其实luby device用了一套API对吧

我用一个I/O control去去查询这个device mapper的能力

然后创建一个device mapper的设备

这个是是关键的步骤

你看他用IO control

然后有一个dm d v create

然后把name啊

UUID啊

这些东西都都都写进去

也是通过I/O control啊实现的呃

分区的映射

再次用这个每为每一个分区创建这个子DM的呃设备等等

然后基本上你看到这边呃

总结下来就都是用用I/O I/O control实现的

就我们可以把任何的软件系统都给打开

然后可以看到里面实际执行的系统调用它是怎么和内核交互的

所以这也是其实为什么说你想要再复制一份LINUX是非常困难的对吧

因为你看起来OK我们像想要实现一个操作系统

实现read write open这些就可以了

不是这样的

你当你碰到I/O control的时候

你发现内核里面的几乎每一个子系统

它的API都是用I/O control这样的东西暴露出来的

甚至有些是通过文件系统对吧

CSFS暴露出来的

那那这些复杂性你都你都需要实现

所以想实现一个兼容的LINUX就啊就非常困难

然后甚至是比如说我们现在的NVM硬件上面它有name space支持的时候

它就有点像一个硬件级的呃

LVL这是一个真实的快设备

但是不管怎么样

就快设备你还是可以这样理解

就虽然有有你知道有有很多的有趣的功能呃

你可以做层层的虚拟化

但最终一个block device就是一个read write啊

可以和和think的呃的操作好

那在这个基础上

就在这样的一个比如说这样的东西上面实现一个呃文件系统

那面临的困难是什么呢

数据因为本质上文件系统就是一个数据结构

你你直接实现它就可以了

你面临的主要的困难实际上是你在实现的时候

如果要高效还是很有挑战性的

因为你你读写数据都会造成读放大和写放大

因为你很多时候真的只想要一个字节对吧

你只是想要这个文件的大小

或者只想要这个文件的st

比如说什么时候修改的这些信息

但是你为了读到这个信息

你就需要把一整块数据全部都读进来

所以这个时候你你的数据在内存在磁盘上面的排布也就也就很重要了

比如说你更新一个字节

你想要更新一个文件里面的一个字节

你不仅要更新这一个文件

你还会更新它的modification time对吧

它的原数据

然后它就涉及到多个块的写入

涉及到多个块的写入

甚至一整个哪怕只是一个块的写入

你都需要首先把这块读出来

把中间的部分写进写再改掉

改掉以后再写进去

而当你说写一个快的时候

你又会涉及到SSDFTL里面的那个map table的修改对吧

它有一个像page table

在SSD里面有个像页表一样的东西

做地址翻译

你你看到的比如说我要写第一块

实际上你写的不是第一块

你写的是第一块映射过以后的那个呃

虚就虚一是一个虚拟地址

映射过以后的那一块好

因为你要维护这个虚拟地址

所以你又有一个数据结构

然后这个数据结构又是copy on right的啊

这个copy right又会在SSD的内部产生斜放大

他如果要拷贝的话

它会拷贝根节点

然后再拷贝拷贝拷贝

所以这是一个呃

理论上说你写一个字节可能会有几万倍甚至几10万倍的写放大

但这个比较极端

你你你真的是只写只写只写一个啊

一个字节呃

写一块的话

依然会有若干倍的读写放大

这个这个甚至可能到十倍都是有啊

有可能的

所以你你们也知道要在文件系统这一层要做缓存

和memory hierarchy一样对吧

我们memory hierarchy里面有一级二级三级的缓存

我们也需要在呃内存里面留一份数据

然后我们的block create和block create就看起来看起来这是一个REDISK对吧

呃我可以对它做be read或者be right

但是操作系统内核会统一分配一个

当然LINUX内核里面这个东西叫buffer cash

是一个很糟糕的名字

你就可以认为它是一个cash

那buffer cash这两个东西连起来

这是这是一个历史原因

呃不应该这么叫哈

它就是一个cash文件系统的cash

然后你你的be read你可以类比成是共享内存

就是内存的load

然后be write是store

你就很好理解了

就跟你的cash一样

你如果漏的话

你必须从cash里面漏的

如果cash miss的话

就说这个cash里没有的话

他就要去这个设备里面找

当然如果设备是这样的话

这个设备又会去这个里面找

这个又会从去去look back device里面

又会到文件里面找对吧

经历一层转发的呃过程写的时候和呃memory hierarchy里面的不太一样

你因为呃我已经是一个block的写了

所以我这个block可以直接写到写到这个catch里面

把旧的东西给覆盖掉

然后就相当于是cash hit了对吧

它存我写直接写到catch里

下一次read我就可以直接从catch里面给呃给读走了

这是呃这是你要做的一个呃一个优化

所以其实呃你看到很多操作系统里面做的事情和另外的呃系统

比如说存储系统

内存系统里面做的事情是一样的

他也用了这种locality的啊原理

如果你想要想要让这个文件系统高效的话

你就希望尽可能的把一起

就我会常见一起访问的数据结构要放到一个块里对吧

很自然的一个想法

这样你你catch hit以后

我这次比如说我读到这个数据结构

我下次要读到它临近的那个呃数据结构的时候

我的catch就会hit

这样可以减少访问的次数

就一定程度上减少这个读写放大

但是有些读写放大还是还是不可避免的对吧

比如说我就是要写一个字节

那那确实会造成比较显著的读写方法

好啊

这是一个呃block device的模型

然后现在在这个block device上我们就可以去做啊

就做文件系统的设计了

好那文件系统我们要实现的是什么呢

我们要实现的就是一个目录数呃

反正任何东西都是跟对吧

比如说杠be杠home

它可以有目录

然后比如说它可以有文件

然后当然可能有链接对吧

嗯在链接的意义上

这个图画的并不是特别好

但是啊这也不重要了

比如说list是一个呃

bt rain好

你要实现一个这个东西

然后用block read block create呃

来实现呃

那为了实现这个数据结构

其实就有好几个东西需要实现

比如说这是data对吧

这是data

然后还有每一个目录或者文件的一个meta data

包括比如说它的大小吧

比如说它的名字名字也是他meta data的一部分

比如说访问它的权限mod

然后比如说他的时间戳呃

上一次我也给大家展示了文件系统

你每次修改以后

i notify的时候修改以后

你是可以感知到呃

尤其它的上层目录也会被那modify time也会呃也会修改

以及这样的一个目录

树状的结构对吧

这个树结构呃

你你们是你们写实现一个数的时候

你们OK你可以实现一个STRUCT对吧

然后你有一个比如说vector of什么什么的呃

求职对吧

你们可能会可能会这样呃

实现一个数据结构

然后类似的你在磁盘上面也需要一个呃这样的呃数据结构

然后呢如果你要实现一个高效的文件系统

OK那呃你可以观察一下现有的文件系统里面的一些呃统计

这个这就是你先需要做一个study

然后去去去厚颜呃

厚着脸皮去找各种人说

哎你的文件系统能不能借我呃

便利一下对吧

我不窃取你的隐私

但是我想要看一下文件的分布

让他去各种场景

比如说数据中心里去这个个人用户里

PC不同的人里面去把各种数据给拿出来

拿出来以后

K就可以得到一些结论

那这个结论稍微有一点呃过时了呃

不过基本上还是符合我们现在的UNIX系统里面的这个呃文件的分布

尤其是个人的一个电脑的分布

数据中心可能会稍微会很不很不一样对吧

数据中心里面大部分都是比如说管理的数据库好

如果只是我们说这个系统文件的话

那绝大部分的文件都是比较小的

也符合我们的预期

比如说我们生产的源代码

我们不可能在一个文件里面放巨大无比的源代码

这样会让你的维护变得很困难

我们还是会把它拆拆开对吧

我们所以我们生产的内容

文档性的内容

它全部都是不算很大的

然后以及有一些像图片啊这些这种BINNER也不算特别大

然后他说啊

few big fires use small space对吧

比如说一个数据库

一个电影它确实是会占用绝大部分的呃空间

然后一般来说一个目录里面的entrance不会太多的对吧

如果你的你组织你的一个项目里面呃

有几千个文件呃

那对不起

这个你要浏览它也会也会比较啊比较困难

当然现在都是AI浏览了

所以其实好像也没有那么大关系了

然后还有一些有意思的发现

比如说像fire system还roughly half

不管你的文献有多大

你你总会想办法把它填的比较满呃

有意思的一些一些findings

然后这些findings就可以指导你去设计一个呃高效的呃文件系统

你你其实就是回答几个问题

第一个这个meta data怎么存对吧

my data是直接存在这吗

还是存在一个别的别的地方对吧

然后data怎么存

然后一个一个文件是一个贝特瑞

那它就是好多个有好多个块组成的对吧

那他的第一块

第二块

第三块

所以这也需要一个数据结构对吧

你需要知道文件

比如说这个文件可能很小

比如2KB4KB它也可能很大几10GB

然后一个很大的文件

你怎么知道它的每一个offset所对应的那个块在哪对吧

这也是一个非常现实的现实的问题

你需要一个数据结构来来存储它

然后基本上你你你你可以做的是便利这个世界上所有已有的数据结构

比如说你你去写一个爬虫

然后把世上所有写的和数据结构相关的论文全部爬下来

然后爬完了以后

你就给大圆模型一个prompt说哎对于这个数据结构

我来实现这个东西

根据磁盘的这种特性对吧

你可以把problem拼起来对吧

我要实现一个什么样的文件系统

优化什么样的workload

这是一个是不是一个好的idea

然后你就把所有不好的idea都删掉

然后你就可以得到一个新的文件系统的实现

基本上你你这是今天的我们的呃版本答案

如果你要做做文件系统的话

好那呃呃其实就这么简单啊

然后我就可以下课了啊

我我我剩下的内容都是呃

相当于再把这些人类已经呃沉淀下来的知识跟大家over view一下

首先你要在一个block device上实现一个数据结构对吧

实现一个数据结构

那它一般来说是一个partition对吧

比如说什么

刚刚才我们看到什么杠DEV下的m m c block啊P啊block0P一对吧

第一个partition好

它也是一个呃字节序列

是一个block block的序列

然后那如果你要存储一个数据结构

所有的数据结构都要有一个入口

你们的链表一般会有一个head

然后你们的呃一个二叉树会有一个root指针指着它呃对吧

然后你的你看各种各样的文件

它其实也是数据结构

比如说bat map

比如说我们上课的时候哎

大家还有没有印象呃

我我为了输出一个图像

在做那个minor broth set的时候

我是我用print f输出了一个图片

那个叫一个点PCM的格式对吧

我就一个principle就可以输出输出一个呃

最后你真的能在浏览器里面看到的图片

但这个很好

这个对于对于我这种简单的想在上课的时候做demo的呃

用途非常非常友好嗯

像这些都是数据结构

ERF他们也都有一个头

然后这个头上比如说ELF会有个magic number说哦

我帮助像fire啊以及加载器啊

识别这是不是一个E2F

然后以及这个header里面有一个结构体

这个结构体有什么ER和entry对吧

它的这个文件的大小呃

呃program header的数量这些这些这些东西

所以首先所有的数据结构都有这样的一个root指向某一个block

一般来说就放在最就是放在最头上

像像这种GUI id partition table

也也是数据结构

所以它也有一个root放在总分区的对吧

这是一个partition

这是爬线

它它不是从第一个呃block

整个设备的第一个block开始的

第一个block一般来说是还是一个兼容的MBR

然后后面有一个GPT的呃data structure啊

这是这是partition的root

相当于是整个分区表的

就整个磁盘的数据结构的root

然后当然基于id pattable会在磁盘的尾巴上面再存一份

它会备份两份

因为如果这个分区信息丢了以后

你要恢复这个磁盘就会比较麻烦

虽然也不是做不到啊

他会把它备份一份

这样呃

如果它损坏了

还你你顺着尾巴往前找

还可以找到它

OK然后在这个分区里面

额头上也有一个数据结构

然后这个数据结构就叫super block啊

嗯就这个名字

所以它就叫susuper block呃然后他也是基本上是一个呃

对整个数据结构来说最关键的呃

block如果如果你要损坏呃

如果只允许你损坏一个block

你就想要对文件系统的使用者造成最大的麻烦的话

就应该把这个super block给给抹掉

虽然这个信息还是可以恢复出来的

嗯哼但是会给他造成一些呃一些麻烦对呃

一般来说啊

你你你的一个super block里就有一个magic number

然后有有这个文件系统意义上的

比如说这个block the size

它比如说你的你的磁盘可能是512个字节一一块

但是文件系统它可能说好这个东西五

它可以按照512个字节的block给给所有的block编号1234

但这样的话如果这个磁盘比较大

这个位数就会比较大

比如说fat三十二三十二位的block number

如果你每一个block只有比如说512字节的话

那你就只有512×2的32次方呃

这么多个最大的文件就只只能这么大

但是如果他不按照512字节

比如说按照4KB对吧

虽然磁盘是512字节

但我可以按照4KB作为我的一个逻辑的款

我我的文件就可以啊

更大一点

这是super block

它里面有一些这个呃文件系统关键的信息对吧

比如说这个文件系统还有还有多满呃

还有多少空间

然后还能够剩下多少个文件可以分配啊

数据结构的起始位置大小这些

然后呃像make fs的时候

如果你trace它的话

它就会看到你会看到它

它就会前面写上这个额数据结构

然后我们的mount命令其实也是根据super block去确定文件系统的啊

系统类型的好

这个没问题

OK那既然你知道这是一个数据结构以后

你就可以开始开始设计它了

那第一个比较容易理解的很简单的设计是file allocation table

File allocation table

它可以在硬盘上面用呃

也可以在软盘上

比如说5.25英寸的软盘

单面是160KB

也就是说160KB就意味着它软盘是一个block

是512个字节

它总共只有320个block

只有320个sectors

只有320个块

你想只有320个块

你要在这个块上面构建文件系统

你的设计思想是什么

你的设计设计思想肯定不是什么fancy的数据结构去维护这个

任何你在数据结构里面增加的复杂性都浪费了空间

某种程度上你你会牺牲一点点多余的空间去换换可能更高的查询效率

但是你不想着做这个事儿

你想要的是极致的空间的呃节约

所以呃file allocation table fat

他就做了

他就是这样的一个设计啊

就是一个非常简单的数据结构的设计

首先它有一个他的super block

我还是看那个K曾经呃

我觉得操系统课给大家带来的一个

就我自己的操系统课给大家带来的一个很重要的呃

学到take away

就是你们可以去读这个手册

因为有一个实我们有个实验呢

做那个fire system的recovery

然后你必须要对呃

比如说fat里面的各种东西感到以前不知道的

因为你们从来没有做过这事儿啊

感到很清楚呃

然后你会发现读手册其实没有你们想象的那么困难

因为因为总是一点一点来嘛

如果上来就给你一个什么PCIE的SPECIICATION

你就直接崩溃了

但是唉这个SPICATION的长度适中

你可以对着这个substation写代码

然后最终你把那个代码调对的时候

你会哎你会得到一些呃比较宝贵的宝贵的经验

但好像今天也不需要了

你先直接AI读这个specification就行了

而且呃AI确实呃非常擅长这个啊处理这种手册

OK3.1有一个呃叫BP p structure

Come on to fat

十二十六和32

我暂时先不说什么是十二十六和32

这是他的super block

然后super block里面就有各种各样的呃字段

OEM name对吧

它可以存储一些字符串的啊信息

Bytes per sector

它可以存储这个存储设备呃

每一个扇区有多少个字节

所以它你看它可以兼容512

1024的各种各样的fat

可以兼容各种各样的设备

不仅仅是一种磁盘

然后他还有一个概念叫cluster呃

也就是我刚才说的

虽然我们的物物理上就是那个磁盘

你访问的时候是按照512个字节呃来来访问的

但是你可以把比如说任意的二的K次方个522个字节

绑成一个cluster

比如说K如比如说K等于二的话

就是四四

那这四个512字节就是2KB

这叫一个cluster cluster

然后cluster变number one

Number

Two

Number

Three cluster

好

这是它的核心的概念等等等等等等等等等啊

一个一个specification

这个文档我一会留着

然后为什么要叫fat文件系统呢

它叫fat

就是因为它核心的数据结构叫做file allocation table

叫文件分配表

它是什么呢

它是一个这个东西

它是一个cluster

I d fat of4096

不要把它叫fat

你想象成这个啊

当然这个是针对比如说针对fat12来的4096

12个bit

12个bit可以可以存储

只能从0~4096

一个很小的文件系统

如果fat16的话

就可以655336对吧

它会实际上它还会再小一点

但你可以这样这样理解啊

32的话就是二的32次方

它就是一个next数组

我们说这个磁盘在fat里面已经被编成好多个cluster了

就是好多个块块

那前面有一些是是呃

不用了

就是刚才那个数据结构的图上面有有一些其他的区域

好啊

Cluster number

One number

Two

Number three

这个是一个cluster

所以什么是next呢

next就是一个链表嘛

比如说如果next of1等于三

就相当于是我把一说一是一个数据嘛

就是一个文件的数据

然后文件数据它的next是三

就是说文件的钱

比如说这个cluster下的是4KB

那第一个4KB就在这

第二个4KB就在这

如果next等于零

比如说next2等于零

如果next2等于零

那么这一块就是free的

它就可以分配

就是说这个是没有用过的

可以分配

然后如果next等于一

比如说next

三等于一负一就相当于说好这个文件就到此结结束了

到这块就结束了

这个文件就只有两块

它最多只有8KB的大小

你只要再存一个文件的size

你就可以知道啊

到底这个第三第三块里面有多少是实际上属于这个啊

这个文件的就是一个next数组

结束了数据结构对嗯

那这样的数据结构对于只有320个sectors

这样的小的磁盘肯定是最合适的对吧

它非常紧凑在在这个next数组

比如说fight12的时候

它的一个指针就是12个bit对吧

12不是八的整数倍

他会把两个12bit打包成三个字节

BT1BT2BT三

我三个BT正好是24bit at

然后相当于是这个12bit是属于上一个指针的

这个12bit是属于下一个指针的

这样它是按bit紧压的存储在这个next的数组数组里面的啊

非常非常的省空间

而且因为你看啊在一个呃320个扇区的文件系统里面

绝大部分都应该是很小的文件对吧

都是很小的文件

所以你很小的文件

你你这样的一个next指针看起来是一个线性的链表

你觉得性能可能比较低

但是小文件完全没有问题对吧

而且你可以把整个因为整个呃fat一个呃一个block或者一个赛呃

Sorry

一个cluster是一起读出来的

这class一起读出来

读到内存里面

相当于你一下子把好多next指针都加载到内存里了

所以你等于是在内存里面几乎是在内存里面便利这个数据结构

所以性能也不是很大的问题

但是如果你是一个很大的文件系统

就是fat这个文件系统在在小的存储设备上面是完全没有问题的

但是如果你是一个比如说到几十个GB的文件系统

那里面可能有几GB的电影的大文件

而如果啊如果这个呃next有两种情况

一种是我的next都是连续的

比如说next3等于四

next4等于五

next5等于六

这就这是最理想的

就是你你你访问一个文件的时候

它的他的这个next是连续的

意味着它在cluster里面存储的一大团也是连续的

但是呢如果你分配的时候

因为你你你的文件会不停的创建新的文件删除

创建新的文件删除

所以这跟memory allocation一样的

你如果这有一个空的块

你可能诶分配走了

过一会他又回收了

时间长了以后就会产生碎片

然后一旦产生碎片以后

他这个FS就不连续了

不连续它可能会指向一个很远的地方

然后再指回来

又指到一个很远的地方对吧

来回跳来跳去

跳来跳去

那如果是一个很大的文件

并且碎片很多的话

就会造成很大的度放大

因为你的fat本身也是也是很大的对吧

fat32呃

如果你的磁磁盘很大的话

你你的fat entrance也很多

然后fat entrance很多的话

你你就可能会会发生的是呃

这段的next你读出来了

它的下一个next在另一个可在另一个这个block里

你就需要把这个block给读出来

然后再下一个NEX又在另一个布道时

你又把next下一个next x读出来

所以你就你就产生了很大的啊很严重的额度放大

对我们其实给大家准备了一个实现去检查fat文件系统的小工具

一个点C啊

这个代码对大家很有用

因为你们要做实验

你们要做实验解析呃

fat文件系统

然后你们的起点就是呃这个示例代码

你看到这是一个就是把文件系统真的是当成是呃数据数据结构呃

来处理的

我们直接用map把磁盘一整个磁盘镜像映射到进程的呃地址空间里好

然后你看一旦我把super block映射

就把整个磁盘映射完以后

我的一个fat32的header

其实就是他的super block

我直接就变成了一个指针了

所以它就是一个数据结构对吧

然后我可以从这个指针里面读出各种各样的信息

什么BISPERSECTOR啊啊

total sectors啊啊

各种各样的什么volume number

Serial number

好

完了以后

我就可以用一个遍历数据结构的方式

这个叫d f s scan

OK啊

就用DFS去扫描整个文件里面整个的呃目录数

然后我可以用它来去读一下实验的要求里面的那个镜像

你可以看到目录的结构是可以正确的显示的

就跟你们的PS区一样好

这是一个64兆的啊

fat32的文件系统应该是fat32对啊

FS32的file system

f s tape是是FX32

然后你可以看到好多的好多的文件

有一个叫DCIM是一个假装是一个相机拍摄的那个文件

然后呃里面有好多的点b mp

然后你看到有些图片

比如说这个83个cluster

从呃464~546

它就是连续的

它就是这种next都是连续的

next3等于四

四等于五

五等于六

这样连续的

但是也有一些文件呃是不连续的

我我创造这些不连续

就是我我有些我会不停的往里面写文件

然后删掉写文件

删掉写文件

删掉就相当于不断的allocate1些随机大小的memory

然后然后时间长了以后

你allocation和free以后

它就会产生碎片

产生了碎片以后呃

你看到的就是呃我的一个文件可能分散在连续的几个部分对吧

从2407~2431

然后2511~2587

对这些都是可以用呃

程序把它当成数据结构啊来解析的

所以没有什么啊

没有什么神秘的好

那文件的存储讲完了

接下来就是目录

目录是怎么怎么怎么办的呢

在这个手册里面也有

在最早的fat里面

它存储一个文件的文件名是用固定的11个字节

你看从0~0到11呃

当然现在叫short file file name呃

十一十一个字节是8.3的格式

所以比如比如说比如说这里有一个我这里有一个比较长的文件名

auto ex e c点b a at呃

这个就是DOS里面

如果他看到这个文件

它就会自动运行它

如果你的C盘呃

A盘的根目录

比如说软盘的根目录下有一个auto e x t点BAT它就会自动执行

它就跟你们的那个什么BHRC这种呃

这种差不多

它在系统启动的时候会它会执行

然后这个时候它是8+3走

它只总共只存11个字节

然后它会默认有三个字节是扩展名

所以看到这个非常有历史感对吧

他说文件最多只能有11个字符

文件名最多只有八个字符

今天你根本就不能想象那个啥对吧

你你随便就可以起一个很长的啊

很长的文件嗯

然后这也是刚才为什么我们那个工具里面看到

看到有那个波浪线一对啊

你看有波浪线一波浪线二波浪线的三

就是因为如果这个文件系统是是在今天创建的

它为了保持向前兼容

比如说在DOS里面

你依然可以访问这个目录

所以它会把比如说你有你有一个很长的文件

那但是它的前缀又都是相同的

他就会把这个前缀截断截断

然后波浪线一波浪线二来区分这些这些这些文件

OK这是这是fat的一个呃有意思的呃特性

我们的目录项里面就是呃目录实现也是一个文件

就它也是一个数据

它也是靠这个next

比如说你你的目录里面有好多好多好多项

就是1万个文件

它这1万个文件就是不停

Next next next next

就它它这个数据结构是也是一个列线性的线性表

然后这个线性表第一他是一个好多好多个这种STRUCT的

这个directory的entry是吧

这叫d entry

目将目录像direction entry

然后这direction entry就是我刚才在这里展示的这个样子对吧

它有它有一个名字

它有一个attribute

诶

这里告诉你啊

他的最低位是代表是不是read only

然后第二位代表是不是hidden

是不是system modem id啊

是不是目录对吧

你看0X10

你可知道这个文件到底是不是目录

所以这个是文件的metadata

它的名字

他的attributes

还有一些呃别的fire size

这个是非常重要的呃

数数据结构的文件

然后然后这样每个目录里面就存了它目录下的所有的文件

所以你看到这个文件系统是不支持链接的

就是一个最简单的呃实现

然后你你你怎么遍历目录呢

你就你作为一个文件系统的实现者

你怎么遍历目录呢

你就把这个目录文件当普通的文件给读出来

然后读完了以后

它就变成变成好多好多个这个数据结构的拼接对吧

好多好多数据结构的拼接

然后他他去遍历这个数据结构

他就知道这个目录里面有哪些文件

然后以及这个也非常重要呃

是叫first cluster

就是这个文件的第一个cluster在哪对吧

它有high和low两个两字节

相当于是一个指

就在这个DIMP里面有一个指针指向了这个文件的第一块

呃

我这个图画的不是特别好啊

呃指针指向了文件的第一个cluster

然后这个cluster它的next就是他的第二块

再next就是它第三块继续next

你就可以把整个文件的所有的内容都给啊

都给便利出来

好

这个就是fire fire allocation table

呃没有任何神秘

它就是一个数据结构

如果是UNIX呢

UNIX的设计者

他在设计文件系统的时候长得比fast要远一些

他会想如果我们的文件很大怎么办

如果我们的文件系统也很大怎么办对吧

以及他想要支持链接

就是在UNIX文件系统里面

它和fat不一样

它是一个图

它不是一个

它不是一个目录数

UNIX是实实际上是什么呢

是比如说我的杠

它是一个这样图状的结构

每一个目录里面

比如说这个是并这这个叫并目录

OK并根目录

然后他有一个点呃

他有个点点是指向他上级的

它有一个点是指向他自己的

这是一个图的结构

那如果你们要存储一个

然后这个每一个点是有它的meta data的对吧

呃如果一个病目录的话

首先我要知道它的比如说time

他的一个他的modification time啊

对他的time

然后他的比如说i know的编号一些一些一些一些meta data

同时我要知道这些边

我要知道这些边

那如果是你们

你们怎么会会怎么存储这个数据结构呢

如果你想要实现一个图

那你最自然的想法就是你设置一个叫struct node对吧

节点

然后你把这个节点所有重要的信息

就是你的那个在这个set里面能看到的

把这里面能看到的各种东西什么呃

你看什么s t mode对吧

S t mode

然后什么s st size

把各种东西全部都都放进来

这就是一个节点所有的meta data

然后边怎么存呢

编在这个目录文件里对吧

编因为每一个这个图的每一个节点

它要么是一个普通

就是一个呃数据文件

要么是一个目录文件

如果它是目录

不管它是目录还是数据

它都有一个BATTERRAY对吧

它我还有一个叫数据结构叫index

然后这个index index就相当于是它所有的所有的blocks

然后对于目录文件来说

我只需要像这个struct node

我就给它一个名字叫

I know

i know是有编号的对吧

i know是有编号的啊

每一个IO都有一个呃编号

然后我们就在一个目录的这个数据里面

目录里存一个map

从文件名到i know的编号的映射就可以了

当然你不需要用一个呃比较fancy的数据结构

你就你就顺序的对吧

你你就把文件名对啊

比如说文件文件名

然后一个i know的

然后比如说一个杠铃

然后一个文件名

一个i node

一个杠铃

你就顺序的存在这个里面对吧

一个一个目录文件

然后这就是边就跟你们平时说在上数据结构或者算法时

OJ要写一个图

要实现一个graph

你们也是这样实现的对吧

呃如果你要再在边上面放一个label的话对吧

目标节点在哪

然后他的名字是什么

这样数据结构整个就就实现了

但是UNIXFS里面它比较有趣的一个设计是这个index

然后这个index它是怎么做的呢

还记得我们说大部分文件都是小文件对吧

大部分文件都是小文件

而且绝大部分文件你都是读一个头

它的次数会比读整个文件要多得多对啊

因为你很多时候你需要知道这个文件是什么

然后你稍微看一下就行了

你比如说我知道这是个binary

是个什么binary

那后面的东西可能我不在意它

所以UNIX希望这个文件在访问头部的时候可以非常快

而又能够支持很大的文件

所以他做了一个联合的数据结构

等于他是做这样的

它你可以这样int加

比如说啊这first block

Second block

他直接把他的这个文件的第一块直接就写在这个i node里

第二块也写在耳朵里

但是他只写十块

应该是十块

那可能13个指针还是12个指针

OK我我我不确定还是九块啊

Doesn't matter

他先把一些就是文件的头部的那些块直接放在i node里

所以直接放在i note里

然后如果这个文件超大了

我就会拿一个块来做间接索引

相当于是一层的间接索引

相当于一个一级页表

然后如果这个一级页表还不够了

再用一个二级页表

一个指针

再用一个二级列表

然后如果这个还不够了

再用一个三级页表

这样就可以索引足够非常非常非常大的文件了对吧

比如说一个4KB的页表页表

它有有1024个页表项

所以你一级索引就可以1024×1024

然后这个就可以1024×1024乘以1024

对吧

呃三三解表

所以这是一个非常漂亮的数据结构的联合

他用了locality

然后然后你你你只要有读这个i know的

比如说你要读文件头

你直接从i note里面知道这个文件头是什么

然后还有一个设计

他把所有的灯都放到一起

这样他就有locality了

你在访问这些IO的时候

一般来说像一个目录里的文件

它都是在i know的的区域里面存储

也是近乎连续的

因为你分配的时候嘛

你你在这个目录里面写十个文件

你就连续分配十个

分配十个

你就大概率会得到连续的分配

然后这就意味着

如果他把S的全部放到一起的话

你一次把这一块给读出来

你就会把相邻的对啊

和他有相关关系的文件一起都载入到内存里

所以在有缓存配合的情况下呃

这个i know的也是一个合理的

可以justify的实现

而且这个实验更有意思了

比如说在呃

比如说你要实现符号链接的时候

你要实现符号链接的时候

他就可以偷这个部分的内存

如果你是一个小于等于60字节的符号链接

那这些索引块是不是就不需要了对吧

索引块你不需要再为它分配一块

而是而是你可以直接把小于等于60

比如说我现在要把A把这个把这个文件

把这个文件链接到什么点点下的A点点下载A总共只有四个字节

总共只有四个字节

我就可以直接偷这一块的内存去存一个

把这块内存当成是一个字符串来存

点点下载A这样我我甚至都不不需要为它分配任何一个数据块

就说我在这里面有一些冗余的空间

可以让我有很多好玩的事情可以呃

好玩的事情可以干

然后我们甚至是真的可以在文件系统里面呃

看到这个这个行为的

我也做了一个这样的工具

你不需要真的去读代码

你让AI去看一下代码的结构

然后他对UNIX事件里面有的工具其实是非常呃熟悉的对吧

这是一个EST文件的呃

可视化解剖呃

工具核心用到的是一个叫debug fs的东西

甚至你不需要就是在你做这些工具之前

你只要有一个概念说

我认为操作系统应该有一个办法把文件系统的那些底层的信息呃

暴露出来

而不需要而不需要这个呃

去我们我们用上层的那些API呃

是可以的

它有一个专门的叫debug f s

然后甚至可以可以输出一个

这是我我的我的demo里面的那个呃目录树的结构

这个看起来看起来比较简单对吧

我可以CD进去诶

然后我可以现在可以做一个链接

比如说啊对吧

然后我可以目创创建一个目录

啊当然我可以写一个文件

也可以做一个链接

然后做完了以后我可以再次打开它呃

你你想要看更多信息也没有问题

你就让AI去去去帮你写

你可以看到啊

这里面的包括link的count对吧

然后你看到这个图结构正确的被被展被显示出来

所以A点TX和B点TXT

然后它的他的block的id对吧

你看这些每一个文件read me的那个block的id

然后这个dump e x e four点PY这个这个文件一共有十十六.1KB啊

然后他一共占用了五个block

然后这五个block在磁盘里面是连续的对吧

然后这是一个符号链接

这个符号链接是in line

在i block里面就是要告诉大家的是

这些事情都是可以做到的

你你只要有心去试着构建一个工具

你就能够呃得到很不一样的呃更深入的理解对OK这是这是UNIX的实现

所以本质上文件系统就是一个数据结构

你完全是应该可以在数据结构课里面把所有的内容都讲好了对吧

我用数据结构的知识呃

就把呃就把文件系统系统给讲了

但是操作系统里面的文件系统还有一个呃非常重要的职责

这个是是你们数据结构课没有的

就是应对崩溃

实际上你们的系统随时随地都有可能crash对吧

可能是power loss

你你这就没电了

没电了

你你一个没有电池的设备

它瞬间就所有内存里的数据就都没有了

你们是已经接受了掉电内存数据丢失这样的一个事实了

就已经习以为常了

但是对于persistent storage来说

他要非常小心地确保

即便你有可能power loss

你的文件系统里面的数据也不能丢

这就带来一个叫crash consistency问题

他说crash consistent也就是需要把file system从一个consistent state

它下一步必须要挪到另一个consistent state

然后为什么crash consistency的实现是很困难的呢

就是因为我们说这是一个数据结构对啊

不管是这个UNIX的文件系统还是fat

还是任何一个数据结构

哪怕你你写一个链表对吧

你要insert一个insert一个X

要把Y映射到X0

那比如说你说好

OK那你N等于X的next好

那你就要做好多个指针的操作

比如说你就说X的next等于Y

Y的previous等于X

然后next等

那呃previous等于Y

然后Y的next等于N啊

没关系呃

Doesn't matter

你们已经习惯了这样写了对吧

更新一个数据结构

插入插入一个节点

那在并发的时候

为了防止data race

你会在这里加一个lock

在这里加一个unlock对吧

这也OK

但你们从来没有想过

如果这个时候这个县城突然被杀掉了

会怎么样

也也就是说对吧

crash consistent有点像你这个线程随时随地都有可能突然消失

那突然消失意味着什么呢

比如说在这

你这样想吧对吧

这个这个数据结构呃

我有一个X有一个new

然后有一个Y好

那我第一步是X的next等于Y好

这个对了

然后Y的previous等于X好

这对了

然后呃一开始是这样的

嗯好这就N的PRIVICE在这

然后N的previous我要改成Y好

然后这时候why the next还是空的

突然在这个时候线程消失了

我写了对吧

我写了1234

我写了1234没了

因为这个时候系统crash了

线程crash了

那这时候这数据库就在一个inconsistent的状态了对吧

Y的next是没有的

y next飘飞飘到飘到零了

然后你如果遍历这个数据结构

这个数据结构就crash了

就是说你你必须要把所有的操作都执行完

这个数据结构才是consistent的对吧

然后你马上就发现这个事情没有你想象的那么简单

比如说如果我想要呃去网数据结构里面写一个内容

打个比方

你要你要append的一个对

你要append的一个44KB

比如说一个呃就一个block

恰好一个block最简单的呃append的一个block呃

那你需要需要什么呢

你首先你需要allocate一个新的block对吧

比如说啊等于40好

然后你需要写data

以及你需要更新那个原数据

好对吧

这是123

然后真正的麻烦在这什么呢

还记得那个SSE上面我们拿放大镜给大家看过

看过上面有一个计算机系统

你的一个IO的请求

比如说这个呃write的请求或者update i know的请求

它只是你当你执行这个block right

be right的时候对吧

这两个呃这三个其实都涉及到be right

这个会把40的这个beat map给更新对吧

因为你说你把第40块给分配出去

你就需要把那个那个40bit it map的对应的40块给制成零

然后你这个也需要一个be right去把这个数据写进去

然后你这也需要一个be right

会把这个i know的那个块写进去

然后你想这些be write执行以后

他都是到一个队列里的

然后这个队列是进了你的计算

你的存储设备上面的计算机系统

你就想象这是一个

如果你这是一个旋转的一个硬盘

然后它有一个读写头

这个读写头会怎么样处理这个请求呢

当然是谁离我先近

我先处理谁对吧

如果我马上马上这个盘片转过来以后

我这个22号请求就在我底下

我肯定是先把它做了

因为反正123是不同的block

我按照123的顺序

按照321的顺序

eventually它都是一样的

但就是因为这个eventually一样

导致什么呢

导致实际上它是任意的顺序都有可能的

任意顺顺序都有可能

如果随时随地发生一个crash

就意味着我这里有三个be right

它任何一个subset都是有可能的

所以可能会发生什么问题呢

可能会发生

我现在i note也更新了

数据也写进去了好

但是allocation的bitmap忘记更新了

这会发生什么呢

哈我现在这个文件i know的对吧好

我说我现在知道i know里面有一个第40块

然后这第40块里面有一个文件

比如说写了hello

这个也是对的

但是这个40块在文件系统的视角上还是free的对吧

如果这块丢了

你去运行这个程序是没问题的哦

hello是在的

就意味着未来有一天这个40块分配给别人了

别人又写了一个文件

然后他把这个hello给覆盖掉了

也是你这个文件好好的

第二天过来发现文件内容变了

没有想到过还会有这种问题对吧

所以说我说打开了一个魔鬼的盒子和并发一样的问题

他会reorder

它会乱序

它和并发里面的那个是是一样的呃

思考方式

然后这就麻烦了

你想呢光这个情况我有三个b right

就是二的三次方等于八种情况

有的情况是安全的

把我这个data写到写到第40块了

但40块还是free的

然后i know的也没更新

所以相当于我就往一个free的内存里面写了一些值

这个是安全的对吧

但是有些情况像我刚才说的那种是是不安全的呃

你必须非常小心地处理呃所有的呃所有的情况好

所以啊确实呃crash是会导致文件系统的呃不一致的

那会有怎么样的后果呢

然后甚至是人类在几十年的时间里面

其实都是在一种裸奔的状态

就是文件系统的实现者呃

他可能知道这个crash consistency问题

但是因为想要实现crash safe的数据结构

可能在1970年的时候显得太太过复杂了

所以就干脆我们不管了

我们不管

然后不管哼嗯哼不管就带来一个什么呃什么后果呢

就做system的人都是这样的

都是先挣钱后还债

然后你你你系统会crash

系统会crash

crash了以后你的数据可能会丢失

目录可能会损坏对吧

比如说super

如果你真的写入的时候是一个打

打个比方

写一个i know的region给他给他写

挂了对吧

他crash的时候有可能甚至就crash的时候有可能是我要写这个二

然后正好我我读写头已经到这儿了

我已经开始写了

结果这个ram掉电了

ram掉电以后

花就把那个VLOG给抹掉了吧

也甚至都有这种可能性

他会得到一个live

一个corrupted fire system

完全有可能的

我们在我当时在美国的lab

他们做了这样的一个很有意思的小面包板

然后这个面包板就可以从VGA里面送一个信号

相当于我就VG里送个信号就可以编程了嘛

然后我从VG里送一个信号以后

可以可以物理性的把SSC的电源给关断

然后关掉以后发现很多SSD的计算机系统其实有很大问题的

它crash以后

整个这个盘就可能会死掉

就是你再也读不出任何数据了

而且你知道SSD的修复是非常复杂的

它和HD不一样

你还可以拆盘对吧

SSD基本上没有办法修复

然后这个工作后来促成了厂商的呵呵一一大批固件的呃修复

反正再后来就再再也找不到啊

找不到这样的问题了

然后所以systems的人呢思路很简单粗暴

反正我的文件系统是能用的

我现文件系统是好用的

这种小问题我先不管

然后如果出了问题怎么办呢

你可以事后送去修电脑

做一个file system check

当然fire system check的这个后果也是很严重的

呃你们都知道快速格式化的文件系统

你是可以把数据给捞回来的对吧

比如说那个摄影师事件里面

修电脑的最后就被判了八个月的有期徒刑

当然当然这个事可能是在你们出生

是不是在你们出生以前的事情了

这个inspire了我们的那个f s recovery的呃实验

然后你你实际上我们在UNIX的很长的一段时间里面做的

当crash以后都需要做一次fire system checking

你们有没有在小的时候

在你们的机房里面

如果还是还是比较老的windows系统的话

会看到它在启动的时候会有一个扫描文件

系统是正在扫描文件

系统找到多少坏处

然后把它们都修复了对吧

本质上就是你的数据结构

有可能比如说刚才你的40块是free的

40块是freed

在beat mt里面

第40块是是free的

但是在ANNODE里面有一个40块指向了一个内容

唉这时候如果你扫描这个磁盘上所有的i know的

你就可以便利出所有已用的相当于是已经使用的存储空间

然后如果已经使用了存储空间的话话

你你发现比如说40已经使用了

那这个40就不应该是free的

你应该fix这个这个40对吧

应该fix这个bitmap

然后在很长一段时间里面呃

甚至大家还在做做这种role base的fascism checking

然后你就可以你可以看到他他有他说

如果在你有有些时候是当目录发生移动的时候对吧

你会得到一个很奇怪的对吧对吧

一个一个很奇怪的

比如说第一指着第四

第二指着第四

然后第三的父亲是第一

第四的父亲是第二

一个就你看起来也看不懂的文件系统结构

你必须要猜一个对吧

你必须要猜一个原到底是什么样的

前面的状态呃

导致了现在这个不一致的状态

当然像UNIX

比如说默认的FSCK他就会会说把它丢到一个叫lost and found的目录里

所以你们看到吗

有的时候你会在

有的时候会在你们的文件系统里看到一个叫lost and found final的目录

这个目录就是在你的文件系统崩溃以后

它重新扫描以后

扫描这个一致性的数据结构以后

它有些文件比如说已经没有这个i note

比如说从已经没有任何的目录指向它了

可能是那个目录文件损坏了

他依然可以把这个文件

如果那个i note还在的话

i note还在的话

可以把这个文件给捞回来

会放到那个lost放在里面啊

很有意思的一个fire fire system checking

但是这不是我们想要的

其实不是我们想要的

我们真正想要的是实现一个crash safe的文件系统

它在任何时候它crash都可以

它不会影响我们的一致性

然后这是怎么实现的呢

这还是要回到我们的呃数据结构上好

打个比方

我举一个二叉树的例子

比如说我们现在有个二叉树

一开始是空的

对啊行一开始对

一开始是空的啊

就什么也没有一个二叉树

然后那我们第一次比如说insert3

我们就会得到一个三

再跟上

然后再比如说insert一个六

那六就会在这对吧

六然后再insert一个呃

呃比如说我我们会在这好

那这个时候比如说呃数据结构可能是有parent指针的对吧

有parent指针

那就跟刚才链表一样

你会面临multiple write更新的问题

也就是说你你会你会说在我写这个五的时候

我说五的parent等于六

然后六的left等于五打比方你有你有你有多多个这样的写入

这时候它是crash on safe的

也就是说你对数据结构的写是crash on safe的

但是什么东西你可以容易做到crash consistent呢

Append only

所以数据结构其实有两种视角

第一种视角

这就是实际的数据结构

对啊

你说好

这就是一棵树

然后我我的树上呃

每一个节点有left right

有parent啊

这是这是这这这个就叫数据结构

然后它有一个比如o log的查询时间

我很喜欢它

还有一种方式记录这个数据结构是记住它所有的操作

如果你能记住所有的操作

就算是这个数据结构不一致了对吧

丢了一个

比如说五点PONT等于六丢了

对你也不知道这这个一和二谁谁谁被persist了

不重要

甚至整个文件系统整整个数据结构丢了都可以

你只要replace这个插插入的序列

你就可以把这个数据结构再恢复回来

甚至你可以做一件什么事呢

我可以做一个memory trace

也就是说我对所有的数据结构共享内存的修改和分配

我全都

比如说NM的三

比如说五点left

六点left等于五

然后mam of5点parent等于六

你实际上可以记录的是所有的对share memory的WR

然后你reply这个所有对share memory的write就可以replay任何数据结构

因为数据结构的本质本质上你都是通过memory rat实现的

你只要把所有的memory wr按顺序记下来

你replace这个memory write

你就可以得到一个一模一样的数据结构

就这么简单对

所以你实现crash consistency

他真正做的事情就是一个1+1大于二

我们同时要在block上面存储一个这样的

就是用所有的呃

这就是UNIX文件系统

就是我们刚才看到的对吧

说什么有i know的beat map

然后有i know的

这就是struct node的一个数组

然后全部都在这

然后我在磁盘上面单独开放一个区域叫做JOURNALLY

我看我有没有slides

这样我单独在文件系统上面开辟一块区域叫journal

然后我每当我执行一个be right的时候

我会干什么呢

我会先把这个be write写

以append only的方式写到journal里

然后等我的这个确保我这个我的b right任何数据

这个b write写到这个journal以后

我再去把这个b right再去写到这个数据结构里

然后如果我这个数据结构写进去了

我这个东西就可以丢掉了

但是只要我的这个数据结构

比如说这里还没有写完

那我我我这里始终是on disk的

就把它搬到磁盘上

这个操作还没有执行完

那么这个东西一直存在

所以如果在这个时候发生crash

磁盘上的数据结构是不一致的

但是我re reboot以后

我可以把这些be right at重新再replay1遍

这个叫writer head log或者叫redo log

我我更喜欢redo log

就是说你你你这个log是为了重做

是为了redo的

这叫writer has

在写之前的一个log也叫re re do啊

Redo log

然后这样我们就实现了

就是就是我可以实现一个任意的数据结构的呃

crash consistency好

然后这里面就有一个非常呃经典的数据结构叫呃l s m tree

叫log structured merge tree

呃

大概是大概原理是这样的

你们可以可以回去可以在AI的帮助下下学习啊

这是现在现在的这种像k key value啊

甚至数据库啊都是都是以它为根基的

它基本上是这样的

如果你要实现一个高效的在磁盘上面的数据结构

你你你首先会在内存里面放这个比较热的数据

然后你你如果要写

比如说你要写一个数据的话

你会直接写到内存里

然后这个内存里面有一个

而且有一个并发比较友好的

像skip list这样的啊数据结构

然后你对这个数据结构所有的更新都会以right head log的形式呃

写到写到这样的一个日志日志里面

然后直到直到这个mam table写满了以后

先买回来

当然如果随时这个慢慢呃

这个系统发生crash

那因为我有这个red head log

所以我可以把内存里面的这个数据结构也在也再重建回来

然后当memory table写满了以后

我就会触发一个flash向磁盘的持久化

然后磁磁盘上面存储的一棵一棵树

全部都是不可变的

全部都是不可变的

它都是那种靠均摊分析

有有点像什么呢

我我现在我有一个很大的数据结构

然后它一层一层的

每一层都比下下面那一层总比上一层大

然后最上面一层是memory

它很小

然后memory写满了以后就会触发下层的合并

比如说他发现这个东西写满了以后

我要插入到这一层

然后插入到这一层以后

所以把这一层的两个节点又发现又太大了

又太大

又会合并

它又会到下一层

然后这层如果又太大了

它就会把它再合并到下一层

然后就这样均摊的方式得到一个接近于OE的呃时间复杂度啊

我没有时间来展开讲这个r a century

但是它就是它的核心就是一个内存的层次的存储

加上一个rider head啊

Right have law

对呃

然后在文件系统里面

在文件系统里面

如果你要实现这样的呃

Journally

你你你你其实不需要像我说的说的这样

直接把所有的be right

就就对于任意的数据结构

我都呃都成立的这样的呃这样的日志

而是你可以呃你你可以这样

你可以这样

你你在任何时候

你在想要写入一些数据

比如说啊刚才我们说我们要update一个i know的对吧

我想要update一个i node

然后我想要update一个bit map

就说我的一块数据块被分配了

然后我希望往某个地方写data

我只需要把这个实际的需要写入的东西直接往里写

然后在这里我做一个标记

这个叫transaction begin

说我的传塞成事务要开始了

传塞人开始了

好在这个时候如果crash

如果question时

那那那这个数据我认为就随时都有可能嗯

可能丢嘛对吧

随时都有可能丢

所以所以这些数据都是不可靠的

所以在这个transaction begin写完了以后

我们会执行一个flash

会告诉磁盘说我要等待等待所有的这些数据要落盘

等它落盘

然后等它落盘完了以后

我会再写一个特殊的标记

这个叫transaction and代表事物结束

注意我会先我会先flash

然后再写这个transaction and

然后我end以后可以再flash一次呃

可以再flash一次

这就意味着什么呢

这就意味着是这些数据

所有的这些数据都happens before flash

Flash happened before

我写transaction and

所以说这反过来就是当我在磁盘上面

如果看到了这个traction and的标记的时候

对吧

说明产责人案已经发生了

就产责人案的必须发生我才能看到它

因为产32的发生了

所以flash一定也发生了

而且完成了flash发生了

而且完成了

就意味着这些数据一定已经落盘了

一定落盘以后就相当于是我对数据结构操作已经被记下来了

我只要replay它就可以了

所以我我要做的就是这个叫CHECKPOINTING

就是我我当我把这个东西写进去

并且确保它落盘以后

我就可以把这个操作replay到相当于redo吗

replay到实际的数据结构上

然后replay完了以后

我就可以把这个journal呃给给消掉了

然后在这个时候如果发生崩溃

在这个时候发生崩溃

如果在在flash之前发生崩溃

那这个事情就好像没有做过

因为我不能保证所有的数据都落盘

但是如果在flash之后或者传送人按了之后

我看到了这个传送人按的

那这些落盘的操作

我就可以保证在重启以后可以正确地反映到文件系统上

我就实现了啊

Crash consistency

然后为了实现这样高效的journal

其实trick还挺多的啊

比如说在哪怕是xv6这样非常非常小的教学用的操作系统

你们不知道有没有兴趣

同学有兴趣看一下啊

我今年不讲了呃

他会把多次系统在用的traction合并成一个

就说我我可能有好多啊

好多ongoing in the right

但是呃我可以把这些好多好多on going write

比如说我写了这个文件

写了这个文件

写了这个文件做成一个transaction

这样我就只要flash复杂11次

相当于是我crash的时候可能会稍微丢多一点的东西

但是呃我减少了对磁盘的这个等待它同步的呃的次数

然后LINUX也是的

LINUX里面有一个啊

有个叫journaling block device

叫JBD啊

g b d two是第二个实现啊

你会看到对于每一个设备都有一个这样的啊JOURNALING的呃

demon在后台

他每隔一段时间

比如说30秒钟的时候会会把这个东西做一个snapshot

而不是你你你写一个文件以后

马上就马上就会呃落盘

在在系统里面会稍微稍微要要慢一点啊

然后ex e four很有意思

刚才我们说我们有我们的核心是一个happens before对吧

我们要要做这个flash

然后有因为有这个flash落盘

所以我们就可以可以确保先数据到达磁盘

我才能看到呃

traction and有这happens before的呃关系

然后e x e four做了做了一个呃非常精高明的操作

他说我我不需要这个transition app

我也不需要这个呃

就不需要真的甚至都可以不需要去真的去flash等它flash

你你还是可以可以把这个日志推落盘

这样这样比较安全

而是我在头上直接写一个check up

加一个长度

这样我在crash以后

我不是直接扫描这个journal的区域

然后我看一下哦

这儿有个check up

这儿有个长度

然后我来读一下这个长度是多少啊

啊长度是这么多

它拆some对不对

如果拆some是对的

就说明它正确落盘了

我就reply啊

如果拆some不对

我就把后面的全扔了对吧

就就这么简单啊

这样他就可以省掉

但他用check some的机制来省掉这种落盘的synchronization

然后以及像ye four

它还会做这种metadata journal

因为我们知道数据占磁盘写入的绝大部分对吧

你绝大部分写磁盘的实际上是写数据的

而数据其实丢不丢

通常对你来说都是不要紧的

因为你的程序应用程序一定程度上有义务要保证你

你你不会丢掉数据对吧

你不能在保存一个文件的时候丢掉一些数据

所以呃esc for default时候会采用一叫meta data成他把所有的i know的啊

beat map啊

这些东西的呃更新全部都会写到日志里

但是你对文件内容的呃修改它不会不会反映到日志里

因为无非丢也就是crash

Consistent

意味着你crash了

它会丢一小段时间内的内容

所以它更多的是要确保一个目录数的完整性

它不能因为你的那个目录节点啊发生问题

然后把你的目录整个移到lost and found里了

这你就嗯这对你来说是一件比较糟糕的事

对吧

呃他要保证文件系统的目录结构是一致的

但是数据可能是丢的

这样可以提高性能

真的是这样的吗

答案是不是实现crash consistency

比你们想象的呃要麻烦一点

假设啊

假设你写了一个文本编辑器

就像你们在数据结构课上面呃

做了一个WM的clone

然后你你你做了这样的一件事啊

你你现在你现在在buffer里面

你自己的buffer里面有一个A点TXT

这是memory

那你怎么保存文件呢

你肯定不能直接

首先你肯定不能直接open这个A点TXT

然后带一个OTRUNKET

如果你用OTRUNKET打开它

在open的那一瞬间

你你那个文件内容就没有了对吧

它已经就窗

当用用窗口打开的时候

这个文件就已经没有了

然后这时候假设你的程序程序被杀掉了

进程被别人杀掉了

你这个文件也就没了对吧

文件没了

所以所以你肯定不能用o trunk的去打开一个文件来保存它

所以你会怎么样呢

你会先open一个A点TXT点tamp

然后你会写入这个写入这个新的新的数据好

然后一旦写入新的数据以后呢

你就可以close

ok close以后你就觉得哦我已经把临时数据写进去了

我已经安全了

然后好

你说你remove on link a点TXT好啊

link完了以后你你再rename a点TXT点temp对吧

然后到A点TXT哎

这个实际上是不行的

为什么呢

因为我们的操作系统里面做的是meta data制能力

它对这个东西不负责

也就是说他在写journal的时候说好呃

这个文件创建了对吧

这个有一个o create其实隐藏了一个说我现在要创建这个文件

文件创建了好

这个文件要创建

所以我知道了

在这是个meta data对吧

在目录里面要创建一个文件

这是一个mata data

然后要删除一个文件

这个也是目录里的操作

也会被journal

也会被放到刚才那个日志区里

然后这个时候发生了system crash data是不正常的

所以crash完了以后

系统redo log里看到什么呢

哦我知道了

第一个我要创建一个空的A点T4点tap

第二个我要把A点TXT给删掉好

上完了后一点天气就没了

你回来以后

你重启回来以后

你你的那个保存文件

它就啊哈它就它就没有了

实际上这个现象是非常普遍的

就打个比方啊

比如说get it

get it是不管文件系统的一致性的

因为它是PON里的data structure

他只要确保所有东西都就他所有的object都在

所以理论上说

如果你的文件系统live了inconsistency

它还是可以把把git回到一个过去的正一致的状态了

因为所有的commit object

the true object都不会丢的

它都在文件系统里

所以你扫描一遍

你总是可以回复到

就你没有任何东西可以丢掉

所以gate就说哈

那他绝对不会管他

为了性能

它就不管这个crash consistency

然后这就带来了一个很有意思的后果

呃

我们很长时间os lab都是在呃虚拟机里做的

然后在虚拟机里做的时候意味着什么呢

意味着同学们啊在在编完成以后

比如说今天编程

然后make编译通过

然后make submit

然后在make的时候我会做一次git commit

对吧

你们知道我会做一个git commit

然后commit完了以后就就反正文件系统里面都写进去了

就执行了一大堆这样的文件和目录的操作

然后然后mix submit交上去一看

OJ过了或者交上去了好

他马上就立即点击关闭虚拟机

然后关闭虚拟机的时候有好几种选项

一个是安全的关闭

一种是快速的关闭

那对于同学们来说

我们一定是选择最快的方式关闭它

然后快快速关闭意味着直接给那个虚拟机一个power off

直接那个尖头拔线线拔了

拔了以后

你看我们的文件系统是有日志的那个journal block device的

哪怕是meta data可能都还没有replay对吧

journal的一部分

然后这时候git就会进入一个不一致的状态

因为有些数据就不对了对吧

data没有了

get就会进入一个不一致的状态

你需要重新扫描它

然后所以嗯对

所以我们在用户空间是可以控制这个行为的

用户空间为了实现对吧

如果你想要是在用户空间实现crash consistency

有一个叫sync系列的系统调用啊

有一个sink命令

如果你们去看我们的make fire的话

在你的git commit以后

我会加一个这个think做一个全局的文件系统的同步

确保数据都落盘

所以我们那个make会比较慢

尤其是如果你们也像我一样

是在一个树莓派这样的比较慢的存储设备上

你会明显感到一个肉眼可见的延迟

这就是我为了实现crash consistency

说我告诉操作系统说

think说请你把所有东西都落盘

对吧

请把所有东西都落盘

确保数据的呃一致性

然后这是一个family of system cos

你也可以带一个文件描述符

比如说有think fs可以可以去同步这个文件描述符所在的整个文件系统

也可以就同步这个文件的data和meta data

就这一个文件描述符

甚至还有一个非常有用的性能叫f data sf d

它同步文件的data

并且只有它的meta data变了

只有meta data变了

这个metadata才会才会同步

这个是很有用的

为什么呢

你如果要在磁盘上面实现一个数据库

你很有可能会分配一个固定大小的

比如说对吧

你有一个4GB的database

那这个4GB的database所有的空间都已经被分配出去了

也就是说它每任何一个block都已经映射到了磁盘上的一个一个block了

然后这个时候你对它的任何修改

实际上都不会改变这个数据结构的映射关系

只有数据和那个modification time

然后再是f data sink

F thea sinsin

它会更新这个data

就是如果你写了一个

比如在这写了个123

他会确保这个123落盘

但是他不会去动那个i know的

因为动i know的时候

你你就要又多涉及到一个呃一个快的写入对吧

他不去动

那i do

因为呃因为它的size和它的这个索引都没有变

一个固定大小的大小的文件

所以你做f data sink的时候就是没有就是没有额外的i node上面落盘的啊

开销的

所以你你如果你要在应用程序里面

比如说你要实现一个数据库

你要做一个这个redo log的话

你一般来说呃用f da think的性能是性能是最好的对

然后一个有趣的故事

如果你们都不知道应该怎么保存文件

说明地球上的程序员应该都不知道应该怎么保存文件对吧

你们知道正确的方式是什么吗

有有同学知道正确的就怎么样保存这个文件是安全的

首先你可以在在这里面加

首先你可以加think对吧

可以加think family的系统内容

哪怕是fda think

你把这个数据确保这个数据写进去

然后再执行这个后面的操作都是可以的

实际上正确的方式是只留一个rename就可以了

因为rename的保证它是它能保证tommy city rename的系统

那样会保证你换过去

要么要么他就换过去了

要么他就完全没有换

它是有这个atom信息保证的啊

这是LINLINUX的设计

但是开发者怎么可能知道这一点呢

所以我我当时我在读PHD的时候

我就想除了真正好的system programmer写的软件之外

所有的业余程序员写的软件都应该有crash consistent的问题

然后答案就是yes

当时我们找到了

包括呃就是因为很多是legacy

就老的很老的系统

它它在那个设计的时候根根本就没有press consistence的概念啊

像格鲁的make make可能会leave一个incorrect build

然后sort sort

如果你in place sort的话

那个文件可能会丢掉

然后那个pearl呃

pro也是PERO

PERO本身它是支持那种像SD一样做正则表达式替换的

然后如果它这上面有个杠I那个in place的选项

然后那个也会把你的文件整个给丢掉

然后甚至我们发现的是2016年

那个时候vs cod刚开始刚开始有这个整个JS世界

node js的世界没不存在一个API可以安全的保存文件

也就是说所有的node js的election的应用全部没有

就全部都会有crash这些问题

然后那个那个时候那个GITHUB还是item吗

那item现在已经死了

然后item还有人在那github issue上面说呃

说我要保存一个很大的文件

然后这个时候磁盘空间满了

然后呢我的文件就消失了

然后他就对啊

非常frustrated

很有意思

所以操作系统课我觉得就到底教会了我们什么对吧

比如说我今天讲了呃

日志讲了crash consistent里面有个概念

然后你们学了一组API f synf data sy

肯定是留下了一些印象

然后下次你知道哦

要处理这些数据的时候要非常的小心

但其实我真正教你们的是

就我反复的

比如说我要我要我要开一个trace啊

我要啊web code一个什么东西啊

你们真正应该相信的是

所有的什么东西都是可以做的

所以我昨天就让我的agent

我把我那个paper2016年10年对10年以前的paper呃

丢

直接把paper丢给大圆模型

然后让他帮我实现一个crash consistent的checker

当然是一个简化版的用户

他的版本你看有将近1000行的代码

反正修了几轮基本上就就对了

比如说啊我可以展示一下这个salt这个命令的对啊

比如说我可以salt a点TXT

然后但是我salt又是它salt又是支持in place的

salt本身是支持in place的

就是它是可以呃

有些有些程序不支持的

因为你如果用O窗可以打开它的话

它就直接挂了

但salt是支持的

但支持的时候它是不支持crash consistency的

所以我可以

呃需要统计一下吗

好我可以去呃观测这个salt保存文件的过程

它执行的所有的系统内容和文件相关的系统内容

然后我可以生成很多模拟出来的可能的crash的现场

然后我可以让大圆模型直接帮我去解读这个result点JASON

然后他就可以复现

相当于我是让大圆模型直接直接复现了

呃

我当年当时那个论文里面的研究结果

所以我们的paper是可以是reproducible的

但呃然后我就发现了一个洗paper的流量密码对吧

你可以找到一个还不错的paper

然后你用你的token和超能力去扩大100倍的实验规模

因为理论上说因为有现在有LM了

你可以对吧

你你你可以去世界上所有的开源软件里面都做一遍这个实验

而且你可以用LM去替代所有的heuristics

我那个时候没有LM

所以所以很多时候我需要依赖heresy来判断这个呃到底有没有这个crash

consistency的呃这个问题

所以你看这个AI已经他知道这个salt会在截断以后发生一个崩溃

然后他在写入中断的时候也会发生部分的数据的

就是他能够他能够正确的找到这个呃这个bug

然后当QUOTIS为了possibility

他不打算修修这个呃

per最后修了这个bug

我们完全可以用LMTACRISK

然后去洗一个

就做一个非常好的实验

但是但是大家不要这样做

你们你们学操作系统

你们要去干那个呃对吧

地球上没有人做过的事情

因为现在有有你有知识了

然后你有模型了

你就可以去做世界上没有人做过的事情