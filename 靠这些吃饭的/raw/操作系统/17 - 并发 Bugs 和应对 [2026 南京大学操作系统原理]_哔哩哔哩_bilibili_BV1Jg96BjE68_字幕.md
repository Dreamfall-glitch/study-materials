呃上课之前我先在告诉大家一下

4月30号

也就是周四

我们有一个闭卷的期中测验

测验就到呃

前面互斥同步之前不包括同步的呃内容

然后你们需要复习一下

比如说现成的概念啊

这些啊

我我应该是会考的

嗯大家不用太紧张

第二个如果你们五一有出游计划

同学就可以直接不用来了

我就可以期末替代

但是我发现已经有不止一个同学来问我说

如果呃如果交卷了

然后我期末考的更高

能不能用期末算

那肯定是不能的

这个你你选择交卷

就是你你认可你的成绩了对吧

然后你因为你还不知道期末考试题是什么样

不能什么事情都要呃占便宜呵

这样对嗯

然后并发编程呃

我们讲到其实已经讲完了

因为你们理论上说已经可以用我们讲过的机制

实现各种各样的呃并发程序了

呃我们呃最开始的时候把呃线程比作人

把共享内存比作物理世界

然后这样我们构建了很多的呃并发的算法

像PETERSON的协议啊这些

然后又有spun join lock on log为broadcast pv啊

操作这些API

然后就是因为有了这些API

当然它底层的操作系统

比如说操作系统提供的是呃像clone啊

或者是呃FUTX这样的系统调用

然后帮我们实现了这些呃

并发编程的基础的API以后你就可以去实现

比如说像计算图啊

像dining philosophers啊

啊

生产者消费者啊这样的呃

各种各样实际中有用到的呃

并发编程

当然了

我还是要告诉大家

并发编程其实是很难的

因为我们人类呃本质上我们的整个训练过程

预训练后训练

微调过程都是在物理世界中完成的

然后在这个物理世界中

有一个非常基本的物理定规律

就是我们的脑子是在我们脑壳里的

它它飞不出来

然后我们能够感知到的都是我们自己的手脚

眼睛看到的东西

所以呃人天生就是一个物理世界

这个share memory里面呃视角非常小的一个一个东西

然后我们就被训练成了呃这样的东西呃

所以没办法

我们呃包括我们写程序的时候

你们学编程的时候

一开始也是教你们顺序执行

你们都没有对吧

没有意识到呃

其实程序编程语言是一个顺序执行的呃模型

因为它实在是太自然了呃

然后一旦我们从顺序程序变到并发程序

那就很容易产生程序执行的各种各样的幻觉

你会不由自主的会觉得哎

我的程序应该是顺序执行的

然后就就错了

因为你要理解所有的并发程序的行为是np hard

比呃你必须要枚举指数多个东西

才能知道并发程序真正的行为是什么

所以今天我就来给大家讲一讲

Learning from mistakes

是是我们在实际的并发编程的时候

会遇到什么样的问题

然后以及我们人类是怎么样解决啊

解决这个问题的好呃

首先第一类我要讲的并发bug叫死锁啊

这个名字也非常的直观啊

思索a dead lock is stain

Which each member of a group is waiting for another member

互相等待

including yourself和可以自己等自己呃

To take action

然后实际中死锁也是有可能发生的呃

这是去年11月的时候

我们班上同学的投稿呃

这是真实的发生的事情

呃

我觉得这个词索要发生其实条件也挺苛刻的

就是那个公交车头尾头尾头尾连起来了

然后呃没有一个车可以呃可以在啊

在make progress

所以这就是一个很自然的呃并发

并发当中也可能遇到的呃问题

然后思索有很多种类型

比如说AA型的思索

什么是AA型的思索呢

同一个线程我可以我我之前讲的时候都是说哈

如果我们要呃正确的实现并发编程的话

比如说我们有线程要做sum加加

那我T1我我就lock对吧

当然一定要log同一把锁

然后sum加加

然后unlock

然后在这里面lock和unlock

我们假设都是配对的对吧

我们讲信号量的时候说有一个骚操作

是我们在一个县城UNLOG可以把锁

然后在另外一个线程一开始main的时候

把这把锁线快了

然后在这把锁release

在这个线程T1线程release

这样做一个happens before

但实际用的时候我们应该是这样的

比如说T2也是一个lock同一把锁

然后呃那这时候他就等待对吧

要等到它unlock以后呃

我这个锁才能重新获得

然后SAM加加

这样就可以实现互斥了

但我没有上课的时候没有讲的是

如果你lock同一把锁

比如说lock1把锁

任何一把锁

我再做一次lock会发生什么呢

对啊你们觉得会发生什么呢

就你们站在操作系统或者

API的设计者的角度

你们觉得会发生什么

你有两种肯定有两种实现嘛

一种是呃你lock的时候有一个计数器

你你lock一次就加一

再lock一次就减一

那这个计数器是可以是县城本地的

因为你只有这把锁以后

你再维护这个计数计数器就可以

然后你你这时候你因为你有两个unlock

你就必须要呃有两个lock

你就必须要有两个unlock配对起来的

才能啊把它呃给解开对吧

你第一次unlock你只能把这个计数器减一

但减不到零

然后你只有第二次unlock的时候

你才能

那这个行为到底是不是到底是不是这样的呢

你看我都告诉你了

这叫AA型的呃思索

如果你们产生过这样的问题

那就比较好

你们在学这个线程库的时候

哎我我觉得大家在学呃这些东西的时候

我比如说我没有讲的时候

很自然的呃

你们可能会想到这个呃

但是以前编程验证是一个很麻烦的事

但现在你让AI直接写个程序

然后我我今我也让AI直接帮我写了一个

写了一个死锁演示的程序

比如说我让他做一个AA型的呃

Dead lock

能够acquire

再再acquire

那这对于AI来说是一个特别简单的呃任务

比如说a a dialog啊

那我我我就放一个提log

甚至我可以在main里面做

然后我就是呃acquire

然后再打印一个消息

然后再acquire同一个

然后我再打印一个消息

那如果呃不会死锁的话

那这个线程就会结束

然后整个main函数也会也会结束

但如果我们编译运行它的话啊

看一下诶这个程序就卡住了

所以死锁是一个还算是它的symptom

是比较容易检查到的

你你最直观的就是

你你预期你的程序总共就做一点点事对吧

做一个生产者

消费者打印100个括号

那你对打印100个括号这件事是有时间感知的

应该是在你能够感知的时间之内

它马上就结束了

但是你看我的这个线程

它你预期比如说0.1ms就要结束了

但是它到现在还没有结束

所以你你你知道可能发生的问题

而且这个问题是你第二个message没有打出来

对吧

你刚才我们的代码里面

应该如果能够像像这样是计数器

线程式计数器的话

你应该能看到两个啊

Print f

但是现在没有

那我们就知道啊

确实发生了呃丝状

然后其实啊我我我经常

我现在经常在AI写代码的时候

我会观察他写代码的过程

而其实每一个同学你们在AI写代码时候

也也可以像我上课这样

因为你看我我我是可以给呃cloud code grant权限的

比如说我可以给他读写的权限

这样他就再也不会提示我

然后我也我也可以

甚至这个dangerous skip all permissions

就是他他不不给任何提示

然后他删库就删库

最近好像有一个什么

就在这两天就又有一些谁被CLO的删除了

然后导致这个生产环境里面下线

然后导致回滚到备份嗯

呃你完全可以说今天做操作系统实验了

然后直接coco的帮我搞定

然后他甚至自己可以去网上读那个实验手册

对吧

他可以fetch url l

还可以甚至可以browser youth

然后他最后可以自己提交

然后提交完它甚至还可以自己browser youth

去去找到它

我的online judge没有过没有过

他可以再fix嗯

但是这个其实你你就失去了呃

你你学习里面比较重要的

重要的一环就是观察和思考呃

如果你来做

你会不会这样做

如果你来做

你是不是做的比AI好

有的时候我们也也经常看AI犯蠢

我就真的有的时候很想打断他

CTRL加C打断他

然后骂他

然后骂他

他他就会说这个you are you are absolutely right

对吧

说这个呃呃呃但我也可以不打断他

然后我可以观察他嗯

是怎么一步一步把这个问题解决的

就今天的AI很明显的呃

他的能力就是他会犯错

但是他他犯了错

他可以把这个错给给修好

这这这是一个很厉害的呃能力

然后在这个过程当中

AI就不断的向你输出信息

它会包括有思考的中间过程

你甚至可以在你的cloud m d里面加一个prompt

让它呃多把他的思考过程给讲出来啊

以一个老师的身份来来向你解释啊

然后再继续呃

那这在这个里面你就可以学到

比如说像库函数的使用啊

写代码的best practice

然后你你你可以看到

他用了各种各样的命令行工具

有些可能是你从来没有用过的

你你知道哦

你留个印象

原来你在做这件事情的时候

有可能能用到这样的一个呃命令行工具

那这种AA型的思索

你看起来就很傻

对不对

我的那个例子里面呃

我lock a

然后我再lock a

你你你你打赌自己绝对不会干这件事的呃

真实的世界就比较复杂了

因为真实的系统里面你不是只有一个文件

不是像这里一个over sisimplify的case里啊

你只有一个线程

然后这个线程只有十行的代码

当你的代码散落在几十个文件里的时候

然后这几十个文件里又会有多层的函数调用

甚至递归的时候

还可能会有隐藏的控制流的时候啊

call back信号中断

你在处理你

你现在acquire一个lock

然后结果这时候你按下CTRL加C

然后CTRL加C的时候

你认为这里有个并发数据结构

你要保护好它

你也acquire同一个lock

然后就形成了这个AA型的思索

也就是说是我们的一旦程序开始并发

开始异步这个控制流

你就不可能完全想清楚了对吧

所以所以我们需要互斥这样的机制

来阻止并发的发生

但是阻止并发的发生也是有代价的

你会产生这样的呃思索

你们回可以回去问问AI有没有

现实中有没有这样AA型思索例子

才会告诉你很多

而且有些是真的是在生产环境里面呃

导致一些导致一些问题的好

除了AA型的死锁

还有一个很重要类型的思索

就是ABBA型的死锁

那这个就更更直观了

也就是我们在那个哲学家吃饭的时候呃

我我们看到的情况对吧

所有的人都举起左手acquire1把锁

然后他试试图学quire另外一把锁的时候

就没有人可以make progress

就跟我们刚开始的时候

看到那个公交车的例子一样

所以如果有一个线程lock a

然后他在lock b好

那如果线程T1和T2

它没有没有交错执行

那这个程序好像OK先A后B

但是如果T2他lock b

然后这个时候T1和T2都持有了自己的锁

那这时候T1试图去lock b

他就要等到T2把式锁释放掉

但是同时T2如果这个时候执行lock a

它就需要等待T1把这个锁释放掉

所以就形成了一个循环等待

当然这个在在Danny philosophy里面呃

这个问题更复杂

因为他有呃五个人

五个县城会形成一个环境的等待

不仅仅是ABB

但是呃简化来讲

它就是一个AABBA型的呃dead lock嗯

你们如果做实验的话

你们拆锁的时候还是很容易发生呃

这样的ABBA型的思索的

在实际的呃呃系统当中

ABBA型的思索也是最常见的啊

我这就不用展示了对吧

这个带din philosopher呃

如果我我不去像上上一节课

我讲的那个有些聪明的办法去解决啊

上锁的这种环形的等待的话

他就打一会

你就看到这个plosers is eating

不再打印出来

整个系统就陷入了一个静默的状态

不过好消息是死锁

一般来说比较容易检查对吧

因为你很快看到你的程序输出了一秒钟都不停

在输出日志

突然就一一个日志也没有了

哎你马上就知道了哦

你的系统里可能发生了一个死锁啊

而且在这个时候

如果你也touch一个GDP上去的话

死锁也是一种相对来说比较容易呃检查的呃

bug对

这就是思索好

然后为了解决这个死锁问题

因为刚开始最早的时候操作系统开始有了

然后大家就开始开始写有锁的程序的时候

就各种各样的出bug

然后出了bug以后

大家自然而然的就会去研究呃

思索到底是为什么会发生的呃

然后我们怎么怎么避免它

那这里有一个呃我也是我很喜欢的经典论文啊

叫system deadlocks

1971年的呃

文章我倒是真的是推荐呃

每一个同学去去读一读

像像比如说我现在用豆包读

读这个所有的论文啊

他会他会帮我读好

我就不打开了

但是这这这个文章写的

你看1971年写的非常平实

然后你可以从中呃得到很多的呃启发啊

我们还是死锁

是是是一个典型的并发问题

然后我们理解并发的时候

我们也是呃就是把线程想象成人

然后把share memory想象成物理世界

那我们就把锁理解成桌子上有一把钥匙

然后我们能只有拿到那把钥匙的人

才可以继续执行代码

也就是说才可以进入这个啊临界区好

那这个paper里面就提到了

死锁产生的四个必要条件呃

所谓的必要条件就是任何一个条件

如果呃不满足

就这个思索就不会发生了

就可能不会发生了

当然还可能有别的呃别的问题好

那第一个条件是说呃是mutual exclusion之互斥

也就是说你你实现的

还是要还是要实现一个互斥

一把钥匙只能被一个人拿到

拿到钥匙才能继续

就说互斥

如果你这互斥不互斥了

那那肯定是也不会死锁了

锁都不是锁了对吧

你你你你的呃包间对吧

门上的锁都被拆掉了

那你你你你确实是不会思索了

但可能会有其他的问题

然后第二个条件是wait for

意思是像这种情况

拿到钥匙的人还想拿到更多的钥匙

这也很重要

因为你看AA型的四座也是的

我已经我已经把这桌子上的钥匙拿过来

放到我口袋里了

我还想再从桌子上摸一把钥匙

我已经永远不可能做到

我就AA型的思索了

然后ABBA也是的

我有一把AA桌子上的钥匙

我拿走了

我还要去拿B桌子的钥匙

然后如果这个世界上就只有一个桌子

一把钥匙

而且拿到钥匙人还永远不想要更多的钥匙

就A没有AA型的思索

那确实就是一把大锁保平安了

哎确实也不会思索了

然后那个呃NO pression

别人不能抢

我不能抢别人的钥匙

在我们的程序里面也是不存在

这个抢别人钥匙的这个行为的对吧

抢别人钥匙行为我们做做不到

因为我们所在实现的时候

那个system call从从你的我实现一个自旋锁

我不停的去看这个钥匙在不在

我的协议已经规定好了

我我我就没有抢别人的锁

这个操作

OK嗯

然后循环等待

最后一个也是啊最直观的

我们要形成思索的条件

那么所谓的必要条件

就意味着打破任何一个1234里面的条件啊

死锁实际上就不会呃不会发生了

好那么呃你们可能会看到有一些呃

有有一些资料啊

资料这个是百度百百科的死锁词条

我我每年都会拿出来鞭尸嗯

然后但是我我我总想着

有一天也许这个百度百科就会改进啊

结果是实际是不仅没有改进

而且还变得更糟糕了

我不知道

我不知道为什么

而且这还是一个应该是个验证过的

认证过的词条

本词条由科普中国百科词条编写工作

项目审核呃

我就不不多不多评价了

连里面有这样一句话

我我我昨天copy过来说说理解了死锁的原因

尤其是产生死锁的四个必要条件

就可以叭叭叭叭叭叭叭叭叭叭叭叭叭叭叭呃

因此对资源的分配要给予合理呃

合理的规划

我不知道

可能是197几年的时候

有一本教科书上这样写

我觉得如果你读过那个system deadlock呃

那个文章的话

嗯在那个时代背景下

如果你读过那篇文章

你会觉得呃这段话写的是没有问题的

但是现在已经2026年了

呃这样这样竟然还出现在百度百科上

所以我我觉得挺挺好玩的挺好玩的呃

中文的语料就是天崩开局

然后我不知道deep sick为什么可以训的这么好

我我我就天崩开局到什么程度呢

我大元模型有一个分词器呃

它你你任何一句话对吧

一句话它总是要切成一个一个字符的

当然它不是像像那个UNICODE或者像阿斯克一样

它切成一个一个的字节

或者一个这种十六十六bit的东西

他会把一些常用的词呃

甚至是带标点符号的

带空格的

带句号的

这些都都变成一个token

一个叫词源啊

现在好像大家要要这样这样翻译啊

然后这样的话我就可以实现呃

我把常用的词放在这个词表里

把不常用的

甚至有些东西就以字节的方式来

直接编码在里面

那我常用的词我就可以节省这个token的长度

我我不就可以省算力

加快速度

反正好处有很多呃现在流行的两个呃

呃现在比如说啊在呃在GP上面

这个分子表有两个

一个是四欧的时候出的O200k base

还有一个是O200k harmony

然后它里面最长的这个词源

分别有十个中文字和九个中文字是微信公众号

天天什么什么什么和日本

什么什么什么什么什么

然后这嗯我我刚才让AI帮我那个tick token

共找到3260一个

长度大于等于二的纯中文token呃

大概在整个token里面可能11%左右

1.5%左右呃

就如果这个东西优化了

其实还中文的那个上下文长度还可以更更小

因为很明显我们有很多呃

在实际上我们对话的时候会出现语料

但不在他的语料库里

你看都是呃

我就啊我的直播间还在吗

我已经对吧

我我可以让DPK来来做一些啊啊comment

唯一的这个比较长的词里面

一个正常的是给主人留下些什么吧

那听起来看起来这句话不太正常

但实际上它是来自一个呃

就是博客的那个底下留言框里面的那个那个词

所以他的语料里面有大量的这个呃博客的post

因为这个已经算是相对来说

质量比较高的语料了

如果你们对比一下那个WIKIPEDIA的死锁的词条

他就写的非常专业

他呃他也有这个必要条件

但是他对呃这个必要条件的陈述是很明显

是更客观

更公正

更符合一个computer science从业者的

所以你看过滤掉这些SPA模板

只展示真正的中文中中文词汇

OK呃我们还是要逐条解析一下

这个刚才说的几个几个条件

第一个mutual exclusion呃

说互斥钥匙只能被一个人拿到

拿到钥匙才能继续

这个就是护士所的定义对吧

所以如果你想要打破这个条件

不是像刚才那一句话

站着说话不腰疼

他说在系统设计进程调度方面

注意如何不让这四个必要条件成立

就这句话你说的他说的是对的吧

你没错吧

你你你不能

你肯定不能说他错

但是它你就觉得它不客观嘛对吧

就像我那天在知乎上写了一个帖子

说人工智能AI有一个呃呃中文的名字

不要大家不要叫AI了

但人工智能又太长了

然后我就有了啊

我我就找了一个字

然后这个字非常好

然后这个字非常好

非常适合这个当前人工智能的呃

四个四大含义嗯

我就不说这是什么字了

然后我就觉得这个这个什么制

就是就应该应该是人AI的

这个中中文翻译又又短又上头对吧

但这不work对吧

这不这不work呃

你你需要的是有一个呃reasoning model

一个仔细考虑的呃

model去想

为什么这四个必要条件打破起来是非常困难的

第一个呃钥匙只能被一个人拿到

拿到钥匙才能继续

这个就是颠覆了锁的锁的定义

如果你想要打破这个条件

你就需要代码设计的彻底重构

你你就不能再就不允许你有锁了对吧

不允许你你你用用互斥锁了啊

你还是有办法的

比如说你可以呃

在din philosophy里面引入一个waiter线程

然后你可以跟他通信

你可以在系统里面引入一个send和receive的API

当然你send和receive的时候

你可你还是需要用锁来实现

但是你一旦实现了send和receive的机制以后

你就呃不再也不需要用锁了

你只要跟这个waiter通信

所以你看我们现在分布式系统这种呃

master worker的这种结构

它就是呃用这个方式来设计的对吧

它打破了呃

不去呃

不去互斥

这样的不去就直接使用锁来来互斥

然后自然而然也就通过集中式的方式来管理啊

解决了锁的问题

他如果要解决拿到钥匙的人

还要更多的钥匙

这个问题就也是比较有趣的

首先我们可以一把大手保平安

然后你只要消灭了AA型的思索

你你就再也不会有其他的思索了

然这是对的

但是它的性能很低

这也可能也不是我们想要的

但是有没有又性能好

又能一把大锁保平安的方法呢

其实早就有人想了

他们想在C或者C加加

或者任何一个语言里面啊

我引入一个新的关键字叫atomic

就跟你你们说有一个database传在场

你们不知道这个没关系啊

就叫TOMIC

然后我在这里可以写任意的代码

比如说SAM加加

甚至啊他们呃理想情况下

我这里可以可以写系统调用

但是系统调用就像泼出去的水

你不可能再把它收回来了

呃atomic意思是这一一些代码

它执行要么好像全部执行完

Stop the world

全部执行完

要么好像一下都没有执行

所以如果我这里面

比如说我这里有两个sum加加

然后我在另外一个呃县城里面

我也同样的写一个tomic sum加加

那你看到的就只能是2468十

你永远不能看到基数这样的一个or nothing的语义

这样像支付宝我就可以在里面check对吧

我可以我可以先对全局状态做check

然后再再modify

这样我可以保证这是一个all or nothing的呃

结果

那这这就是对的呃

非常难实现

产生memory

非常难实现

尤其是如果你要有系统调用的话

这就更更难实现

但这是我们呃其实是人类追求的目标嘛

越困难越要做呃

我觉得在AI的时代

可能这件事情又要兴起了呃

也也说不定

然后如果说我们希望第三个条件被打破

也就是说不能抢别人的呃钥匙

抢别人钥匙很混乱对吧

你你想我现在在一个lock的状态

我比如说就是一把大锁保平安

然后那我我在这个时候我想要改改东西的时候

我要把别人的锁给抢过来

那我就要导致别人的回滚对吧

因为别人已经把萨姆加

我中间有两个sum加加他拿着锁呢

我如果要把他这个锁抢抢回来

要么就我得等等他做完

要么我就等他做完

好像不对对吧

那我就得把它这从房间里面踹出去踹出去

我就必须要把他已经对系统产生的side effect

回滚

所以又指向了transaction memory

必须要能回滚

持有所线程执行过的操作

我才能打破这个没有抢占的这个条件

所以你看这是一个非常深刻的呃

而且也是很好的讨论

我们讨论打破锁的四个呃条件

为什么那个system deadlock是一篇经典的文章

不不是因为它最终导致了百度百科上的这个

这句大空话

而是你如果仔细去思考里面的每一个条件

你都是打开了一个新的领域的分支

他们可能让编程这件事件事情变得更好

你仔细去想

然后你你为了实现这个东西

你就不知道要费多少的精力

你你有有可能上千篇的文

paper都都是和这个传session memory有关的

那就是一个很好的这个呃

推动人类进步的一个idea呃

好那回到最后我们能不能不形成循环等待

也就是说我们刚才讲的ABBA型的死锁

如果我所有的锁

我给世界上所有的锁都编一个编号

我强制要求你在上锁之前或者上锁的时候

必须要从小到大上

是不是就能避免思索呢

答案是yes对吧

我上次留了一个小小问题对吧

就是就是lock order人为什么是对的

我们在写Danny philosopher的时候说

呃

我们所有人都举起左手叉子

唯独那个边有一个人

他要先举起右手的叉子

我就可以满足lock ordering了

满足lock ordering以后

在任何时候

系统里面的锁都是有限的有限的

所以在任何时候总有一个县城

他手上有一个编号最大的锁对吧

可能稍微有一点抽奖不算很抽象啊

比如说我我就从左往右划

比如说T1T2T三好

那我们现在假设他们是每一个县城的锁

都必须是从小到大来活

所以我我现在提

比如T1拿一个一没关系好呃

那如果比如说T2也想要一的话

它就必须要等待了对吧

T2就对不起

T2就block了block

然后好

那现在T1拿了世界上编号最大的锁

然后T2可能比如说现在他要二

他也他也拿到了

然后他拿到二以后

T2就变成拿到世界上最大的锁的线程了

如果T1这个时候想要再拿二

那对不起

他也要block

但是世界上总会有一个线程

拿着那个锁的编号是最大的

这个线程百分之百是可以继续运行的

所以我不是种不会发生死锁

对我我总有至少有一个线程是可以往后执行的

然后往后执行以后

这个线程eventually

它会把所有的锁都释放掉对吧

一个线程不可能无限制的呃

持有一把锁

虽然你们在写代码的时候

也可能会出现这个情况对吧

忘了release

那它就那也死锁了哈哈

但至少这种ABBA型的思索

在lock ordering下是可以呃完全避免的

而且这个证明是可以扩展到log group的

也就是说我我我不仅是可以一个锁对吧

也可以是一组锁

我只要按照从小到大的组号来lock那一样的

我任何时候都有一个呃线程

获得一个只有一个组号最大的那个那个锁

那这个线程一定是可以往前执行的

OK那就呃那也是可以的啊

当然你还要保证组里面没有ABBA的问题

但这个相对来说是是更容易啊

容易保证的

所以这这件事情呃

是整个呃死锁四个条件里面

最容易打破的就是循环等待

反而你们觉得死锁

因为死锁定义就是循环等待

但是你们使用锁的时候最容易遵循

就最容易打破的条件其实是循环等待

只要你在编程上面能满足这一点就行了

当然在编程上要满足这一点

本身还是有很大的困难的

有一个啊rice theory

我告诉你

你如果只给你程序

你想要分析出其中任何一个non tribute的性质

它都是to bring complete

因为你可以随便在每任何一个地方

嵌入一个什么证明

验证哥德巴赫猜想的这个程序

然后它永远不会结束对吧

如果嗯你也不知道这个猜想对不对

如果你能分析出它这个会不会思索

你就呃你就可以证明哥德巴赫猜想

这样对

OK嗯

OK实际上啊在现实世界中呃

lock caldering是非常广泛啊

广泛的应用呢你你可以看到在linux kernel里面

在这个memory subsystem里面

这个是r map点C

这是r map点C里面的呃

文档文档

告诉你它按照什么样的顺序获得获得锁啊

这个文件很长

你们可以可以让AI帮你读啊

这我我我已经展示过怎么样直接打开一个cloud code

然后让他呃读这个内核的代码啊

我就不读了

但lock ordering确实是一个可以工作的

在实际中工作的呃

比较好的一个方式

可以啊避免大型系统里面的思索

但是但是这有一个问题

刚才我说你想要证明一个锁呃

一个锁的实现它没有死锁呃

还是非常困难的呃

在科隆里面也有这样的一个文档

叫unreliable guide to locking

他说教科书告诉你啊

如果你apply这个log ordering

那你就不会思索了

但是这有一个假设是你写的程序是对的对吧

但人天生就容易呃容易写bug

所以他有一些这个best practice

比如说如果你一个锁永远不暴露给别人

你只是在你自己的这个subsystem里面使用

它天生就是满足呃

就在叶子上

总是在叶子上使用

使用完以后再再释放它

就它就绝对不会呃产生啊产生死锁对吧

它有一些这个工程意义上好的呃

好的时间以及呃后来啊后来大家也发现

比如说呃大约53%的有文档的

docking locking rule的呃

锁的使用

实际上文档和实现实际上是不一致的

因为你你你你改了一份代码以后

你测试通过以后

你很有可能忘了去改那个注释文档

或者什么地方

因为你你写程序的时候

程序员的上下文窗口是非常有限的

他没有办法看到整个项目

他只能关注到这个文件和一两个文件

如果他忘记他

他在一个月以前还写过那个文

文档里面还有一个地方描述这件事

那很有可能就忘了改啊

但今天不会再发生了对吧

我们有一个足够长的上下文的大圆模型

然后能把所有相关的东西都塞进来

那他一一次就可以把所有的地方都都修好呃

所以呃也许这种这种问题也就成为了过去了

不过再成为过去呃

我们都认为啊

都认为呃bug是不会消失的

因为写代码本身还是很困难的

尤其是写并发程序也是很困难的

没有人能够阻止程序员写出dead lock

也没有人能阻止这个AI写出data allog

所以呃我故意把这个写写反过来了

因为呃现在很火的叫harness engineering

叫驾驭工程

呃驾驭工程实际上是一种软件工程

但是我我我故意把它反过来

软软件工程的本质就是一个harness engineering嗯

这有一个有一个原则

是大型软件系统设计的基本原则

做最坏的假设

做最防御性的编程

也就是说不管372 11

我先假设你的程序是错的对吧

west代码永远是错的

然后除非你竭尽所能

你都没有办法向我证明他是错的

我才能相信他

somehow是对的

你你绝对不能呃说是我我哎呀

我有一个呃程序啊

今天期末考试了

然后呃写完程序以后直接就交OJ对吧

你过了样例就交OJ编译

通过就交OJ呃

这在这个大型系统当中是一个很不好的呃

习惯呃

我觉得当然在AI的时代

大家已从第一门课编程课开始

应该已经扭转了这样的啊

这样不好的很不好的习惯了

那我们为了实现这个呃驾驭程序员

驾驭复杂性呃

这样的软件工程呃

在锁的方面我们可以做什么呢对吧

做最坏的假设

如果我我我会假设你程序员写了lock

他一定很有可能会在某一些罕见的路径上面

忘记养老

所以呃大家会规定

你程序不能随便按你随心所欲的方式写

比如说linux kernel里面就有一个非常好的pattern

它会go to release

就你在你在任何时候

你尤其是这个对于C代码来说

它可能损失一点点性能

但是呃这里有一个有1go to

有一个release

然后这个re release它会检查你所有申请的资源

在这里面allocate的buffer

因为你你在在运行的时

你在程序在运行的时候

你不停的会什么申请buffer

然后会acquire lock

然后在这里面

任何申请都会在release里面检查它有没有被释放

如果没有被释放

它会再多释放一次

然后所有没有被释放

如果没有释放

它也会多多释放一次

然后在任何时候你想要return的时候

return是非常危险的

因为return他只是说好这个战争就没了

战争没了

这个资源还留着锁

还占着buffer还没没回收呢

buffer没回收就是一个资源浪费

Leak

但是你们写OJ的时候

你们你们是让他让他随便立刻的

这也是一个不好的习惯

所以你看到return时候要非常紧张哇

这个return的时候

我是不是把所有的cup都做好了

所以你不要return

你就go to go to release

Go to

有的时候你们觉得不喜欢

但是在这个这个时候

在C里面没有其他的编程机制来保证的时候呃

你你看到这个go to release

你就可以知道放心的

它自带的一个意思是我放心的

需要释放所有的资源

然后安全的啊

安全的返回

当然呃这不是一个

当然这也不是一个万能的方法对吧

你还是会忘记

比如说你在release的时候

忘记释放一个你申请的buffer呃

这也是非常有可能的呃

所以C加加rust有一个非常核心的机制

是用r a w i double i resource

Allocation is invisualization

你创建一个对象

你创建一个对象

然后你有一个呃在任何一个scope里

你甚至是不需要在函数集

你在一个代码块集

你你们呃在代码块级定义自己的局部变量

也是一个很好的practice

让局部变量不要立刻到它的作用域外面

然后你可以lock object在C加加里面在进入这个它

它编译器会帮你保证呃

在进入这个代码块的时候呃

这个构造函数会调用在离开这个代码块的时候

不管你是用return离开的还是别的方式离开的

它都会保证这个析构函数被调用

所以呃所以憋气干了很多脏事

但是这个时候你可以在构造的时候

快在虚构的时候release

你就可以确保这个锁在这个作用域里是生效的

这也是一个非常好的practice对吧

它可以阻止你啊上了锁忘记释放

然后刚才那个例子里面是linx kernel规定啊

必须有明确的lock ordering

你你必须按照这个呃确定的顺序来上锁

否则啊

否则你肯定会会各种各种出问题

但是但是这不是软件工程

或者说这不是真正的HONEYSSINI

真正的HONEYSON经人就是做最最最最坏的假设

做最最最最防御的编程

也就是说他假设你所有的这些东西都得用lock order

你都得有

但是没有一个程序员是真正信得过的

这这这才是我觉得跟AI编程的一个呃

相处之道吧

呃尽管啊尽管可能因为你的精力是有限的

所以你不可能做一个完美的harness engineering

但是你在呃随着项目增大的时候

你越来越需要一些

哪怕是给你增加这个harness的agent

都是很有很有价值的

所以如果你假设没有程序员都是信得过的

那你要找找的是什么呢

你找的是这种ABBA型的死锁对吧

并发程序难

或者说给你们造成麻烦的原因是

你的ABBA型的思索

可能99.99999%的运行里面

都是先A后B

但是你在一条非常罕见的路径里会先B后A

比如说刚才我说的什么CTRL加C的时候

你会先B后A

然后但你平时你可能每天也就CTRL加C

那么几次

然后只要没有发生这种AB对吧

你需要一个你需要一个lock b插入

在正好插入在它们之间的时候

这个ABBA的四座才会成立

但是这个代码可能

这个lock a

lock b可能只占程序执行的0%

点001的时间

你根本碰不上把lock b插入到里面

所以这个代码你测反复的测试

经过了反复的测试

依然觉得好像没有问题啊

诶结果你到发布出去以后

当你有1亿个用户的时候

就总有一个人CTRL加C的时候

把lock b叉插入到这个里面

他就思索了

这就是麻烦

就是你当你的系统从你一个人的玩具玩具

反正crash了

crash就crash呗

嗯并发了

哪怕你有一个race导致了错误

这程序crash了

你甚至不管它就行

你再再起来跑一遍

但是你如果有1亿个用户的时候

那就是一个很大的frustration

甚至这个可能会把你的服务器卡住对吧

你的你一个press就可能导致你

你你给全球所有人的服务都中断了

OK所以你就有一个聪明的想法

如果你还要做更坏的假设

也就是说没有任何程序员是信得过的

那你就不如每一次acquire和release

你都用print f打一个日志

然后你这时候你看到的就是线程一行AB

然后lock b lock a

然后AA呃

a unlock a b unlock b等等等等好

然后比如说T2也是ABBA等等等等好

这都是对的

但是你只要有任何有一天

你这个程序可以运行很长很长时间

只要任何有一天你看到一个线程上面有BA

哪怕在实际上他们都没有形成这种插入进去的

真正的思索

这件事情都是非常危险的

即便你的程序有可能是对的

比如说中间有一个线程的啊

SPONN和SPOND的同步关系

你你在这个线程A线程里面对吧

你在你在T1里面是先A后B

然后你创建了B

然后T2里面是先B后A

假设只有这一种情况啊

这确实没有

确实不会产生死锁

但是本着不要给程序员增加负担的呃

这样的基本原则

你依然要判定这个事情你不要这样写

你永远不要这样写

永远不要认为你感觉他是对的

你确实证明他是对的

你的项目会改的会变的

有一天你把这个T2挪到这个代码

这段代码被别人复用了对吧

复用了以后这个就错了

所以呃做最坏的假设

做最防御性的呃编程

所以我们可以做什么呢

我们可以用LDPRELOAD这个机制

我们可以用l d pro的这个机制呃

在每一次mutex lock的时候

或者unlock的时候都改成我们的代码

我们我们实现过那个LDPRELOAD的hack对吧

呃做过一个修改器

然后这个动态加载的时候

我就可以覆盖操作系统原有的这些啊API

然后那我就可以把lock on lock的日志

像s trace一样打印出来

打印出来以后我就可以管道给一个check点PY

那他就可以正确的呃

正确的知道你的系统是不是可能有浅带的思索

然后这样的一个程序就叫就叫LOGDP

当然我们可以完全可以做的更好一点对吧

我们可以一边trace1边检查

我们不需要把它再管道给另外一个程序

我们本质上是维护一个数据结构对吧

我我我每一次呃

我我的世世界里面有好多个锁

我的世界里面有好多把锁诶

BCD好

然后呢如果每有一个线程

比如说T1T1先上A再上C对吧

我就会把ACD这个这个这个给记下来

然后如果我我比如说我线程B我先先上先乘二

先上B再上D

然后再有一天有一个线程先上了D

再上了A这个图里面就形成了一个环

我们本质上是一个数据结构问题对吧

有没有发现一条新的边

然后这个图里面有没有还一个动态的数据结构

所以啊你你用数据结构的方式就可以

可以可以解决解解决它呃

呃你就知道有没有潜在的这种循环等待型的呃

思索这里这里我也有有有代码

我们有有lock trace和和lock tap

然后这个lock trace啊

我完全没有管啊

这是web coding的

你们有个概念

你们知道这个LDPRELOAD可以呃

override动态链接程序里面的啊符号啊

OK然后剩下这些呃呃麻烦的细节就交给交给他

不过你我还是强烈建议你们学习一下啊

这个细节是是怎么呃怎么办到的啊

呃呃

比如说我可以呃

呃trace一个正常的上锁解锁的程序

Acquire release

然后我也可以啊

trace这个philosopher叫din philosopher

每一个线程acquired拿一把锁呃

然后我我也让AI

直接帮我实现了一个能够做LOCKTAP检查的啊

函数跟刚才一样

刚才一样

刚才是是我们把lock unlock给print f打印出来了

我现在你看它

它直接呃用一个非常暴力的方式维护这个图

然后他去检查这个图里面啊有没有变

不知道他用的什么算法

但这个这个是你们算法课要学的啊

内容这个也不是很重要

重要的是你可以用LD呃PRELOAD的方式

比如说一个

能够通过的test对吧

这时候我就没有日志了啊

他有有64个线程

然后有100轮的上锁都没有

都没有发现啊

什么显著的问题

但是如果你是philosopher的话啊

这次你看这次我运行philosopher

它触发了一个真正的思索

但是我的lock tap其实已经在前面

提前就就发现这个问题了对吧

在第二次运行的时候

我的程序正常退出了

没有发生思索

这也是很正常的对吧

嗯呃如果我如果我运行它

你看我运行好几次

这个程序看起来是没有问题的

你会觉得诶好像不错嘛

你甚至运行了

就你做压力测试时甚至会运行100万次

1000万次

你你觉得这个程序都挺对的

就像你们的MELOC

我不知道你们同学有没有遇到过这个问题

你们你们可能你们你们给他了

一个非常stressed的

你认为非常stressed的workload

但是你就是没有触发那条罕见的罕见路径啊

OK对不起

你就你就挂了对啊

但是LOCKTAP你看我每一次运行的时候

他都能够检查到ABBA

这就是因为我每一次上锁的时候

我都会会找到所有上我这个线

我这个我自己这个线程上锁的顺序

并且把它加到啊

加到图里

OK啊

软件工程的本质就是行业engineering

好

这是第一种问题叫思索

然后你就会想好

那如果我永远都不上锁

那我不就彻底避免了死锁了吗

还记得这个必要条件里面的啊

第一条对吧

一个钥匙只能被一个人拿到

拿到钥匙才能继续

我没有钥匙也能继续

这不就没有锁了吗

好如果你永远不上锁

你就会面临一个更麻烦的问题

叫data race啊

叫数据竞争

然后我现在正式引入这个概念

但其实我们在前面讲各种各样

并发程序的例子的时候

其实我们已经碰到很多data race了

第一个例就是那个支付宝

如我判断

如果我的余额还够

我就把它减掉

别的县城也在改这个余额

我在读读到他写的值了啊

这个阿里阿里配的例子

还有我们两个线程做sum加加

这也是一个data race

然后我们的PETERSON的算法或者说那个协议是吧

我们往往门上面贴字

然后把手举起来

你去看它

这也是data race

什么是data race呢

不同的线程同时访问同一个内存

并且至少有一个是写

OK为什么一定要至少有一个是写呢

是这样的

我们有两个线程啊

T1T2好好

如果我这个线程读X

我这个线程也读X呃

我们不管假设过去发生的事情都是对的

或者说都是好的

那我会保证T1和T2会读到同样的值对吧

过去假设事情已经过去了

但是呢一旦我至少有一个线程式写

这就麻烦了

他们两个就会赛跑

谁跑得快

谁跑得慢

会造成一个nondeterministic非确定性的结果

你看如果我的毒跑的比较快

我毒跑的比较快

就意味着我的血跑的比较慢

我就会发生这样的情况

如果我的读跑的比较快

我就读不到这个写的值对吧

比如说原来X等于零

我这X等于一

那我这时候X就会就会读到一个零

但是呢如果我反过来

那如果是斜跑的比较快

只要快一点点到这里WX等于一

那么这个值就会变成一

也就这个就是我们最讨厌的就是你的并发程序

这个值你说X是多少

你不知道

有可能是这个

也有可能是这个一旦你有两种可能性的时候

你想要理解这个程序难度就指数级的暴涨啊

甚至还有这种week memory model的这种行为对吧

我们说说我我我我那个线程呃

写X等于一

写Y等于一

我读Y读X

结果我还读到的是旧旧的值

明明至少有一个已经啊已经写过了

然后写和写的raise也是一样的

所以你看如果我一个线程写X等于一

我另一个线程写X等于二

就意味着谁跑得快

谁先到了

谁的值就会被别人覆盖掉

我跑得快的值反而丢了

程序运行的快慢会影响那个最终的结果啊

这个一般来说啊都是尤其是如果在你没有保护

没有明确知道他会有什么后果的情况下

都是非常危险的

所以在C和C加加里面

这样的所谓的data race是undefined behavior

untify behavior是一个非常严重的后果

就意味着他把你的电脑炸了都可以

它可以对吧

你你一个安全

如果有一个安全漏洞

它因为因为C语言是直接是编译到指定的

你那个进程就直接在执行

那如果你你的内存corrupt了

有可能发生的是

你你的PC会跳到一个

他在你的地址空间里面写的他的一段代码

你的任何代码都可能执行

你理论上说

如果你的机器

比如说还接了一个核弹发射器的话

你可以把那个核弹都发射出去

你对物理世界造成一个不可逆的伤害

所以这就是undefined behavior

On only fafine behavior

非常的危险

你你基本上用就是你你用就是你错

K呃

C和C加加

当然C和C加加是一种不负责任

也不能说不负责任

他是为了兼容它

是为了兼容任何体系结构

任何呃任何系统实现任何编译优化这些呃

他干脆就把把data rise定义成商地盘behavior呃

实际上你们非常小心

然后java试图定义rise的一个可能的行为

比如说他呃没有人写过42呃

那他最好希望你读的时候不要读到42

它有一个最小的保底

不会出现这种很糟糕的安全漏洞

这样的奥迪反behavior啊

这data redis是一种非常典型的呃

因为你忘记上锁而导致的一种bug

但从这几个例子来看

你们会觉得哦

DA瑞斯是一个我不太可能发生的事情对吧

我怎么会蠢到不上锁呢

其实不是的

你最常见的错误是这样的

你们不是不上锁

你们也知道任何共享的数据结构都要保护

但是当你的数据结构越来越复杂的时候

就像一个链表

我前面讲数值锁的时候

你就log data structure

你你为了访问这个节点的时候

你不仅要持有它的锁

你还必须要持有它的previous和next的锁

否则你对它做任何结构性的修改其实都是错的

所以你们实际上发生的是呃

第一上错了锁对吧

我在一个线程里面lock a

另一个线程也lock b

你看起来是互斥了

但其实没互斥

你这两个SAM还是可能会发生这样的呃

Data race

不管是像像SAM加加这个就是又有read write

又有write write的呃

Data race

然后第二个case是你在一些比较罕见的路径上面

就忘了上这把锁

这也是很有可能的对吧

比如说你可能在肯JY那个handler

在异步事件的处理上呃

在那个路径上

你你你忘记了这个代码还会并发的

你忘了上锁

它就形成了一个呃data redis

可是undefined behavior

它很有可能会有呃比较呃毁灭性的后果

当然了

你也会说无非就是那两种情况嘛

我只要保持大脑清醒就行

不过我觉得现在比如说你们要实现一个

像那种高性能的MALLOC

你你想要上锁

比如说你第一遍没做对

你你只要让AI直接仔细的分析一下

每一个地方应该上什么锁

哪一些是共享变量

哪些是共享数据结构

它真的能跟你做对

所以这还是还是件很离谱的事情啊

以前让你们要受很多苦的

我

我我觉得应该有很多同学

偷偷让AI帮你们修代码了

反正他一下就修了

然后他修了以后也就是加一行代码对吧

加一个lock

加一个unlock

然后那代码就神奇般的对呵对了呃

曾经我们要花很大的代价才知道呃

但是AI很清醒

他大脑很清醒

以前我们呃我觉得这也很有意思啊

以前我们要花很长时间呃

给同学建立这些概念

什么是data race对吧

在什么时候会啊发生data race

我们呃如果一门课讲的像百度百科一样

它最大的问题就是你们不make sense

他说的每句话都对

但是你不理解它背后的呃

比如说我现在要讲说哎

你们遇到的都是这种情况啊

上错了所忘记上锁

但是在实际当中他们是怎么发生的

只有你自己被这个死锁

或者被这种数据性人坑过

你才知道哦

在那种地方会有一些犄角旮旯的SAM加加你

你你确实是很对人来说确实很容易忘记呃

然后我们以前的办法就是让你们做实验呃

做实验的时候

你们会体会到很多有趣的data arrace呃

可以说是曾经呃操作系统系统课埋下了一个我

我在操作系统课里埋了一个最高光时刻的bug

这个bug是和操作系统的代码联动的

和框架代码联动的

也就是说你的当我们说data race

它是一个定义对吧

它这个定义是很简单的

你也能够想象这是一个bug

但是当我们说内存它是D

可以是地址空间里面的任何一个指针

它可以是一个全局变量

可以是一个堆区分配出来的新的内存

可以用的内存可以是一个占的内存

站上也有可能发生data race

而所谓的内存访问对吧

也不是你你你自己写一个共享

不仅仅是你自己写一个共享数据结构啊

OK那个东西是内存访问

那个肯定是内存访问

你你会想办法把那个内存访问的所给呃加好

AI也能帮你加好

但是你的内存访问可能发生在别人写的代码里

可能发生在库函数里

就你你写的代码和别人的代码发生RASE

甚至可能是一条return指令

所以我我我曾经我以前上操作系统课的时候呃

有一个lab

那个lab平平无奇

平平无奇

大概说你要实现一个操作系统内核啊

当然是要在多处理器上正确的

但是在多处理器上正确

一旦同学们发现

你允许把一个先进程或者你理解成线程啊

T 1t2

把一个线程从一个CPU移到另外一个CPU的时候

怎么都做不对

它会发生非常神奇的奇神秘的重启

当然AI应该是能够把这个bug给呃给找出来的

这个bug是怎么发生的呢

发生的是我那段代码

我那个X86的代码

在中断返回的时候会借用借用这个线程的栈

中断返回的时候会借用这个线程站

因为最终呃你你们做过

如果做过X86的EMU的话

你知道的它有一个艾瑞有一条i return指令

它reon five

这不一样

它有条i return指令

会从呃会从站上面把呃

像PC啊什么的给给pop出来

然后这样才能回到操作系统

或者回到别的地方去去执行好这条指令

这条指令在这条指令执行完之前

执行之前

这个线程其实已经

数据结构已经被挪到CPU了

所以你就算是那个数据结构并发的数据结构

你上了锁

这个时候CPU2上这个线程已经开始

已经开始执行了

然后开始执行

就意味着这个线程可以用这个站了

然后这就形成了一个栈和栈的data race

然后这个一部分代码是由同学写的

一部分代码是由这个我的框架代码完成的

然后这个data rise

他们呃会会有会吃非常多的苦头

然后我一般会在课上会讲这个例子

然后这个例子也非常难解决

我需要一个同步

我需要等到这个艾瑞特指令执行类完了以后

T1才能在另外一个CPU上提起来

但这件事情是办不到的

因为你在i return之前

你不能持有任何锁

这是一条指令对吧

这是一条指令

然后再release1把锁

当我把这个数据结构在另外一个CPU上面

把它添上去的时候

这个就已经可以

已经它就直接就可以运行了对吧

然后就在这个解决这个bug的时候

你们就会发明一种呃很有趣的同步方法

叫read copy update

那要等到一个正确解决方法是

我可以等到这个线CPU上

完成了一次完整的线程调度以后

这个T1才能调度在另外一个CPU上记起来

然后这也是LINUX内核里面

现在一个非常到处都有的一个LINUX内核

大概花了可能有10年的时间

把呃很多的锁迁移到了这样的一个呃

几乎在读的时候可以无锁的这样的一个read

copy update的啊机制

一个非常非常有趣的一个小故事

但这只是告诉你这个复杂性

不要把一个简单的定义

不管是死锁的四个必要条件

还是比如说data race这样定义呃

想象的太简单

在实际中呃有无数的坑在在在等着

大家好

这是这是data race

所以呢

所以这个本质软件工程的本质就是哈里森林

哈里斯坦业的本质是是软件工程

再一次我们对于呃data race这样的bug

同样也是可以写一个程序来检查的

但这个写这个程序就比你们要写一个LOCKTAP

这样的程序要复杂了

因为LOCKTAP你只需要看锁就可以了

但是data race不一样对吧

还记得我刚才那个例子吗

我们说lock a sum加加

然后你这就是sum加

这是内存访问

然后你你如果这里有一个lock a some加加

我这里unlock

它就不是一个data race对吧

但如果我这里不是A

我上的是B

这里没有happens before

它就是一个data race

像这种偶发的data race

它在时间上面可能已经隔了很长时间了

它中间可能各自已经执行了非常多条指令了

但我们依然想把这样的data rise给找到

那怎么办呢

你不仅需要打印所有lock unlock的日志

你还需要打印所有read和write的日志

这个就麻烦了呃但是啊在理论上说

你你也也能想到

如果你想要检测data race

你就把所有的每一个线程的这个

所有发生的事件都记录下来

然后记录下来以后

你你就知道

比如说这里有对吧

你看这里有个lock b

这里有个unlock b

然后这里有一个呃read sum

然后这里有个write some

然后这里也有一个

比如说有一个read some

然后呢你你还可以为每一个锁像LOCKTAP一样

你可以记录下锁同一把锁上的这样的顺序

比如说lock b

如果这两个都是lock b的话

你就知道这有一个happens before

然后这就变成了data race的检测

就变成了你现在有一个图

你每一个程序里面的事件都是按顺序发生的

每一个事件就是一个节点

然后然后有有每个线程都是一个total order全序

然后在县城之间

一个锁的unlock会happen before下一次它的lock

然后这个它的unlock又会happen before

下一次的另外一个线程的lock lock b好

那如果在这样的一个图里面

我能够找到两个

一个write

一个read或者两个write

它满足它没有一个happens before的关系

我就找到了一个data race

然后这就是你们现在现在可以用的stress sitizer

我我这里有一个呃这样的程序

这样的程序我

这个瑞士点C我故意买了一个

和你们平时写代码的时候有可能会一样的

在一条路径上面漏掉一个呃lock

然后你看我在有1/10的概率

我在这里会有一个没有保护的SAM

加加对啊

其他的SAM大家都是log保护的啊

所以你你可以看到它运行多次

它的结果呃

是是是是不一样的

然后这时候我们说软件工程就是honeys engineering

如果我们能够记录下所有的这些事件

并且我们可以检查这个是不是有没有happen before

关系的

read write或者write write的啊

Data race

那我们就可以把这个问题提前给找到啊

那么我也可以让AI对吧

就一句话

因为我知道我知道世界上有一种工具

你甚至都不需要知道这个fire sanitizer的名字

你你可以说compile ron c with a race detector

去看它有没有啊

data race这样的这样的bug

然后AI就可以就可以知道他

他要给你一个叫F3DTX等于12的flag

对它

它是因为它的环境变量和我的环境变量

隔开了我

我现在我这里是有有有环境变量的

对他已经编译成功了

他开始运行了啊

然后运行了以后

他就他他发现我的我的树莓派不支持

不支持T3

我来看看他怎么解决我

我没有我我之前没有试过啊

我们来看看DPC4这个V4呃pro的表现

他试图去关闭address space

Layout randomization

再试一次

唉还是不行

尝试用QE没有用户模式绕过嗯

你看这就他就会AI就会给你惊喜

诶

还是一个unsupported

但没关系啊

这个程序在你们的X86上可以正常运行

并且它可以啊直接找到一个呃data race

就他就是一个真正的专家

他对这些东西了解比我还要好

因为我我本身我对arm经验其实不是很多

他真的就什么都知道

然后他会带着你1.1.1点解决问题的时候

你会知道很多关于我这台树莓派的

比如说对吧

如果你们是一个呃别的机器的话

它的它的知识

这就是我觉得是未来更现代的呃

学学习方法

你不管最后他他是成功还是不成功

他是甚至是有可能会去

比如说去搜索呃

搜索互联网

我相信这个问题应该是可以呃可以解决的

你看他想他开始做我想的事情了呃

他试图用用clown来clown来编译

他说CN的SR3它的运行时可能处理的更好

我也想过要这样尝试

所以他真的就是就是一个非常棒的老师

特别特别棒的老师

我真的是没有预想到他会做这样的尝试

他去试图试图去改变这个叶的大小

来使得这个sch sentier

他的这个err message能够消掉

就他真的智力非常高呃

一个非常非常好的problem Sol server

诶

他把它跑起来了

但是但是这个跑是不成功的

他是一个segmentation啊

fault他还试图去用static来啊来编译

但我们就不尝试了

在这个过程中

他真的有可能会在很长很长的上下文下

把这个问题给给解决掉

这就是今天agent特的能力

哎呦我我不再尝试了

因为我我不花去上课的时间嗯

这就是呃A证能力以及harness engineering的能力

然后我也让他呃

我我也拍脑袋有了一个想法

就是如果我要在上课的时候展示一个呃

Stratoscientier

但是我今天我在数学派上那个编译

编译q mi的时候呃

我直接把我这个系统编译死机了啊

可能是过热了

我不确定它它怎么样

我我最后没有编译完

所以我那个代码也没有办法演示了

我们实际上是可以用q email去模拟执行解释

相当于你们用ne mu去模拟解释

执行你的任何一个程序

然后我就可以把memory access的trace给得到

再用比如说IDPRELOAD这样的机制

把lolock unlock的序列得到

我就可以非常容易的得到这张这张图

AI也能做到

所以呃曾经啊曾经我觉得把程序当成分析对象

我们不管说做一个LOCKTAP

我们去把acquire和release的序列给打出来

还是呃

我说我用我用这样的方式来做程序分析呃

都是少数人的看家本领对吧

只有这个system programmm才有的看家本领

但是现在我觉得家底都被抄了

就你只要知道你有个概念

这件事情是能做的呃

AI就可以带着你把它把它做出来

呃曾经啊我们这个12SENTIERLOCKTAP

其实内LINUX内核里面也有

LINUX内核里面还有address and tor

它可以检测那个你的memory error

有很多很多无穷多的bug

都是由这个dress and sentic给给逮到了

然后我们曾经是无数的开发者

一砖一瓦的加到这种大型现项目里面的东西

现在你只需要一个很多很多的agent一起协作

然后就可以把它搞定了啊

这个时代发生了很大的变化

呃也让harness and jin变得更容易了

你们需要的是有我有

我觉得有一天这个AI也会有能力

他说他自己要做自己的harness engineering

他就能把一个更复杂的项目给维护好

呃我觉得这是一个很可怕的事情

就是我们都没饭碗了

程序员都没有饭碗了

然后那呃我们被解放了以后

但是我觉得在座的各位都是你

你你们是第一天像我你们模型发布第一天

你们开始用的这些这些同学们

你们会比别人站得更前

然后你们会第一天吃到这个时代的红利对吧

就是这个AI能力的红利

然后你们又能知道这个AI的边界在哪里

然后你能为他的下一次发布在做好准备

然后你就能在这个时代里面找到自己的位置

我是这样

我是这样理解的

OK好

这是这是哈里森命运河和并发并发bug

稍微扯出去一点

因为今天本来就是讲一些比较实际的东西

在这有个故事

我们都说race condition是undefined behavior

当然这个undefined behavior

和那个C里面的不太一样呃

我今天有一个伤人性命的并发bug的例子啊

叫killed by a machine

有一个DEREK25的事故

大概1985~81987年的时候

有一个呃放射线的治疗仪

它能够发射这个高能的X射线

然后这最终他是这样的

它有一个它有很多束光

最后会聚焦在一个地方

然后相当于它可以去精准的选择

你身体里面的任何一个位置

就你身体里面的任何位置

在不伤害你其他组织细胞的时候呃

把里面那个位置给烧穿

就就烧就是硬烧

然后这这个治疗仪有一个事件驱动的并发bug呃

导致了至少六个人死亡

所以这个是一个非常有名的呃并发的bug哦

这个图不太好

Anyway

他是这样的

这个呃呃SACC25有两个模式

有一个模式是低剂量照射的模式

相当于它它以很低的剂量先帮助你定位

然后他可以可以可以可以拍片

然后还有一个是高质量模式

就真正灼烧的烧烧你的模式

但烧的时候呢

它里面有一个这个反射镜

有一个beam flattenner

有一个反射镜

然后这个反射镜呃

呃我不确定它是什么

我它会以一个机械的方式

会移到这个射线源的下面

这样这样嗯

它可以控制进入人体的辐射剂量

但如果在没有这个反射镜的时候

你就你就呃按照那个患者的说法

是他们在接受这个治疗的时候

瞬间感觉到很强的灼烧感

然后可能也没有太久

但是他们逐渐就因为接受了过量的这个照射

然后导致这个呃最终最终死亡

我web code的一个模拟器

没有一行代码是我写的啊

跟刚才那个图是一样的

就是在在上面有一个呃呃有一个射线源啊

然后这个射线有两个模式

一个是electron的模式

一个是x ray的模式

x ray就是呃比较高能量的模式

然后你看到我在x ray模式的时候

那个beam flattenner就会呃往人的那个位置

patient那个位置移动

然后当然如果我进入这个electron的模式的话

这个being flattner就会撤走

就是因为就因为它在高能模式下

需要一个额外的部件

然后这个部件的移动是异步的

你看到了我在移动这个部件移动的时候

我依然是可以啊

比如说我可以啊啊对我现在是election mod

我可以beam on对吧

我be on呃

我我BM的话它变绿了

然后呃相当于现在在低剂量模式下开始照射

然后我beam off它就可以

它就它就它就关掉了

好现在现在是OK的

就就我现在在electron mode

我先做一个扫描

然后我进入XREMODE

XREMODE好

然后进入XREMODE以后呃

这个就来了

然后这个bug是怎么产生的呢

我在这个时候很快的进入低低能量的模式

然后我再这个时候再进入高能量的模式

这时候那个板那个护板在往边flatter

往边上移的时候又回来了

然后再回来的时候回来还没有到位的时候

这个时候系统会有一个提示

现在很危险

对不对

因为我已经切换到高能模式了

但是我的这个FLANTERN还在往这往这移动

然后就在这个时候系统会有一个猫function54

然后它会有一个treatment post

然后但是这个操作员

你们知道那些软件系统一天到晚都出错

所以操作员做的最常见的事情就是continue

然后如果这个时候这个彬彬FRANTENNER不在

还没有到位的时候

没遮住的时候

就发生这个非常严重的呃事故

比如说我我我再来一次啊

比如说我现在写election mode

然后XREMODE好啊

fly flatten那过来了

然后我迅速切换到election mode

再切换到XREMODE

然后我再continue continue的时候

他就直接开始照射了

这个FTER还在动

然后patient就直接接受了过量的疫苗

然后呃就变成了杀人的呃并发bug

然后这样的并发bug就是软件定义一切

带来的悲剧后果

因为我我们是现实中的状态

没有被完整的建模在对吧

没有完整建模在软件系统里

我们说软什么是软件

软件就是物理世界里面各种各样的需求和过程

在信息世界里的一个投影

然后你现在的投影是有损的

因为我们我们就用了几个变量嘛

它有一个flattened的状态

它有一个这个射线照射的状态

这些变量

但是这个FLATTENNER到底在哪里

我们在编程的时候没有完整的把它考虑在系统

系统的程序当中

这就危险了

这个时候就有了刚才那个呃他确实检测了吧

错误猫function54

但是那个操作员下意识按下continue呃

就造成了惨剧

在更早的产品里面

如果发生这个move function54的话呃

它只有电互锁电路实现的呃

Assertion failure

然后这个时候就直接停机了

停机你必须重启才能工作

所以在上一个版本它它还是硬件定义的时候呃

是安全的

但是到了软件定义的时候

所有的逻辑都用软件实现的时候呃

就就出问题了

所以所以你看到这个软件定义

会给我们带来很大的麻烦

比如说现在智能驾驶汽车

智能驾驶汽车

首先它呃它在遇到紧急情况的时候

最好是能给你刹车

但是如果你在正常行驶的时候

他给你刹车

你又很很不爽对吧

你用很高的速度

它突然刹到很低的速度的时候

你对你的

你基本上就等于是一个碰撞级的体验了对吧

这这是一个很大的矛盾

然后随着我们的软件变得越来越智能

哼我也不知道最后世界会啊

会会会发生什成什么样

对

这甚至不是SARA25最后一个能杀人的bug

在这个bug修复以后

他说如果这个operator send a common at exact moment

需要一个特别好的时机

然后在counter overflow的时候

The machine was skip setting up some of the beam accessors

哎

又是一个

我说按下CTRL加C的时候的那种rise condition对吧

所以在并发程序运行的时候

你如果你每一个线程只是说哦

看起来把它当成sequential program去去处理

它

看起来都可能挺对的

而且经过了精心的code review

但是如果你不考虑这个全局的系统状态的话

呃你你确实就有可能有问题

而全局的系统状态又非常的复杂

你你可能没有能力没有精力再去呃

再去再去考嗯

自动驾驶聚生智能的底线

安全就会变得呃越来越困难

我刚才啊刚才提到确实是很危险的一个啊

一个小例子好

那既然有有数据数据

有有死锁

有数据竞争呃

不上锁不就不会死锁了吗

换换换换眼

如果我们把每一个共享内存访问都上锁

真是掩耳盗铃了

对吧对吧

我每次SAM加加的时候

我都给SAM加一个SAM的锁

每次SAM加加都加一个SAM的时候

不就消除了所有的数据竞争吗

他确实不会产生这种weak memory model的这种行为了

但是你哪怕给每一次memory access上锁

你依然没有解决所有的并发里面的问题

当然当然了对吧

呃我们回过头来思考一下

刚才那个伤人性命的呃并发bug

就是因为我们在编程的时候

太容易做顺序执行的假设了

我们的本质就是sequential creature

然后我们在编程的时候

一开始大一学编程的时候

你们的函数返回天然是带一个同步的函数

调用是同步的函数调用

你们甚至都没有讲过

在上课的时候没有教过你们函数调用是同步的

因为函数就自带一个all or nothing的原子性

我当我把控制流交给那个函数的时候

eventually那个函数一定会把所有的事情都执行好

然后返回给我

这就是我们对函数的一个expectation

当我调用它的时候

它总会返回的

而且带着正确的结果回来

如果没有bug的话呃

所以THETC25也是的

刚才那个代码

如果我们那个模式的切换

你看我我在切换模式的时候

那个呃那个机械机械装置在开始运动

如果这个机械装置运动都是在一瞬间完成的

或者说你的所有的操作都是同步的

我机械装置这个开始运动的时候

我就不能执行任何指令了

当然这个对对操作员的体验很差对吧嗯

你如果是瞬间完成的

就没有任何问题了

但是实际上它有一个异步的

在未来这件事情才完成的一个系统的语义

但因为我们在编程的时候

已经形成了所有的事情都是立即生效的

肌肉记忆对吧

我做了这件事

我做了switch mode to x ray

那么模式模式就应该变过去的

而不是说等到一段时间以后

它才呃模式才会变过去

因为我们形成这样的记忆记忆

再加上现在所有的并发控制的机制

都是后果自负的对吧

我们说lock unlock

你你自己没上好锁

那对不起

你就就错了

然后你的条件变量你没有做好

你也错了

所以这就带来了两种类型的呃并发bug

一个叫atomicity violation

一种叫order violation

然后TOMCITY就是你上锁

你可能就算是每一个共享内存都上了锁

但是如果你有一件很大的事情

比如说你就希望sum加加

看到的所有的值都是偶数

但如果你只是lock a sum加加on lok a啊A

然后你再LOKASAM加加

再UNLOKA好

如果你希望这个sum永远是偶数

你就可以在这lock b对吧

Unlock b

那么所有是这样写代码的

它都是偶数

但是你有一天就有可能有一个线程忘记了

你写了一个log c对吧

然后你再lock a sam加加OOK

就是因为这把锁上错了

这个程序是没有任何data rise的对吧

data race的定义是要能同时发生的一对内存访问

至少有一个嗜血发生在两个线程里

它必须要能同时发生

然后这个是不可能同时发生

因为你你所有的sum都是用一把锁

正确的保护的

但是它还是一个ABA这叫ABA

我我本来这个A是希望是atomic的原子的

因为我我写代码的时候喜PENCIAL

我是sepal creator

我自然而然的会觉得我需要一把头

顺序的把它执行完

但是被别人强势插入

我就被打断了

然后这个程序啊

程序就错了

然后另一种是这个呃

Order violation

就是你因为你条件变量或者信号量没有做对啊

或者同步没有做好

你你导致顺序出错了

那也是一个非常常见的啊并发bug

所以就有这样的argument叫threats

Cannot be implemented as a library

唉这也是我们今天可以思考

就是你可以尤其是在有AI的时候

你完全可以让AI用更安全的

更安全的方式来实现并发呃

而不需要用底层的这样的线程库

当然因为我们是操系统课

你们还是学计算机专业的学生

你们你们要知道所有的底层

比如说操作系统里面

是用什么样的技术来实现同步的

所以所以你们还被迫要学学这些

但当你们真正开始写代码的时候

我觉得像stress cannot be implement this library

我觉得这是一个正确的argument锁

首先它是可以用的

我是可以把程序写对的

但是你很容易搬起石头砸到自己的脚

所以我们要彻底杜绝你能够搬石头的

任何的可能性啊

嗯像用一种更高级的语言

更高层的语言来描述你想要做什么

然后用编译器来保证它生成的代码

一定不会有这种奇奇怪怪的并发的问题

一定满足你的啊需求

OK好

这就有意思了啊

当然这是上一个时代的研究工作了啊

as sports082008年对吧

真的是上一个时代20年以前的工作了

后来他在2018年的时候

得到了most influential paper word

他他是呃

确实是他他开辟了这个呃

整个大家都去研究并发这件事的一个呃

很好的先例

他做了一个empirical study

一个实证研究

他去找了像马斯克啊

party啊

在那个时候都是顶流级的

有最好的那些程序员啊

Live office

Open office

可能不算啊

有一些最好的程序员维护的项目

比如说你看MOZA

MOZA的那个firefox rust

就是他们的团队里面出来的对吧

莫斯拉出来的都是很顶级的程序员

他们依然有105个

真实的系统里面的并发bug

然后他们去把这些bug找出来

Learning from mistakes

然后呃去观察是不是存在有意义的结论

所以他们找到结论就说哦

这些像ABA这样的问题

在现实中程序员还是会还是会写出来的啊

比如说比如说这个atomacity violation

这这个例子其实是一个data race

大概是啊

这是一个非常常见的模式啊

跟你们的支付宝支付宝是一样的

支付宝是说我先用衣服判断

判断一个条件是不是满足

然后这个条件如果满足的话

我就可以做了对吧

如果我手身上的余额大于大于那么多钱

我要扣掉的钱

我就可以把这个钱扣掉了

就像这个如果我的这个指针不是空

我就可以往这个指针里面写东西

结果哪怕是你上着锁

你有一个有一个别人把资资源释放掉了

你这个就是一个crash

然后这样的ABA型的问题其实很多

它不仅出现在像像这种是data race的情况

还有什么呢

你可以use are free

你有另外一个线程

你比如说你把这个指针交给别的线程了

然后别的线程或者说你的整个系统

认为这个指针再也不会访问了

它就把它安全的free掉了

free掉了以后

这里面就变成use are free

他去读一个读一个已经free的内存

这是非常危险的

因为你可能会读到不属于你的数据

你就可能有隐私泄露

内核里面有很多这种usa free的这种呃

严重的安全安全漏洞

当然如果你你free的时候

同时把这个指针呃清掉了

比如说改成now了

那这个程序也可能发成一个crash

对这样的问题不仅在你的程序里会发生

在你的系统里面也会发生

比如说啊就刚才那个例子

就阿里额支付宝的那个例子

就是一个叫呃

Talk to time of

Check to time of youth

你检查和使用的时中间是有时间差的对吧

你你if什么肯定选

然后你你其实你是认为是assert这个condition的

对吧

你是希望这个呃sir肯定是的

但如果你没有实正确的实现互斥上好一把锁

那这个search是有可能不成立的

支付宝就是这个例子

所以你看到条件变量是一个非常经典的

非常好用的同步机制

就是因为它可以保证这个condition是成立的

当你用那个while循环

while not condition的时候

这个condition是可以成立的

但是有些时候甚至在系统里面

你都没有办法做到阻止

Time of check to time of use

在UNIX里

包括LINUX里有一个程序叫mail

Send mail

3D mail是读邮件的

比如说我现在我我自己在我的电脑上面

我回你们

你们有同学可能给我回过邮件

我有我是在命令行里面读邮件的

然后我也是直接

如果这个邮件是我想要简单回一下

我这样AI直接就帮你帮我回了

我是在命令行里读邮件的呃

那个时候当然也是在命令行里组织邮件

然后这个命令行的邮件客户端他会这样检查

他说他会3D mail

回前会先检查你的用户的没有box

是不是一个符号链接

如果不是符号链接

就说明它确实是你用户的这个呃一个邮箱文件

对不对

邮箱文件

一个mail box文件

那我就可以把我就把一个邮件

我的邮件就呃判断到这个文件里

这是对的

有意思的操作是什么呢

我希呃他要创建一个这样的时间窗口

理论上说也是有可能的

呃我我我作为一个攻击者

我没有root权限

我只有我这个杠home a b c的访问权限

我只有这个mail box的访问权限

然后我就不停的干一件事

我给自己先发一封邮件

然后或者说在很短的时间里面

我首先要等一会

我我首先要不要把这个mail box

我的MAD box变成一个正常的文件

然后我再做一个符号链接

指向一个系统里面危险的文件

比如说什么杠etc杠password

你们如果看我现在的这个啊

不是LS

如果你看的话

你可以看到我系统里面所有的用户

也就是说往e DC password里面加一行啊

判断一个邮件

就意味着我可能可以往这个系统里面创

添加一个新的用户

我就把这个系统给攻破了

这个用户可我这个用户可以属于某一个对吧

很有权限的组这个文件

这个文件我作为一个普通用户啧

显然是不可不行的对吧

我我随便往后面写一行

这绝对是不允许的操作

嗯好那通过这个符号链接

我就可以让这个高权限的程序root啊

有有root权限的send mail的程序

首先他看一眼是吧

time of check哦

这个文件是安全的

可以写

它不是符号链接

是属于用户的文件

我可以把邮件写进去

不会有在这个时间点写入文件时

不会有任何的严重的后果的

因为这个邮件本身就是邮件发给这个用户的

我可能甚至自己发给自己的

但是这个文件在自己用户的那个目录下

它是一个普通的数据的文件

我写进去不会不会有任何影响的

但是当我执行到这的时候

当我实际上把这个文件开始打开的时候

我的条件就不再成立了

我这已经是一个符号链接了啊

就理论上说就是无法防御

因为你的system call就是这些

你没有system call可以支持你原子的对吧

检查这个文件是不是符号链接

再把它打开

你做不到

你只能在你只能通过修改这个程序的架构对吧

不能我我不在root权限运行

这样来来来解决这个问题

这也是一个TOMCBLAATION

也是有点像一个race对吧

有点像那个杀人的那个样

只不过这不是一个线程级别的race

线程级别是两个呃

两个线程访问同一个共享内存呃

谁跑得快

谁就会把这个值啊

有另外一个人就会把这个值给盖掉

但这是一个呃资源级的

也就是说我我在操作系统里面有这个对象

然后我现在在访问这个操作系统里面

对象的时候

产生了个AABA

一个tomcity violation

你我想要的是这个的原子性

但如果违反了啊

这就是一个并发的bug

所以兵发bug真的是千奇百怪呃

你很难想象

然后还有各种各样的

像这个呃呃事件没有按照未预定的顺序发生

比如说一个concurrent的use of free对吧

这是这也是一个非常经典的

我我需要一个同步

我需要的是呃

我的free需要等到所有可能的user都结束

我有一个join

我现在才呃才能free对吧

你可以用信号量来实现

你你知道比如说你你知道一共有三个线程呃

有可能会回use死

然后呢我就必须要P3次

那个信号量

相当于我要从那个桌子上拿走三个玩具

然后每一个县城呃

最再也不会访问了以后

他会把一个玩具放到桌上

但你你你可能放的时机不对

你在放完玩具以后

你还访问它

就就构成了一个concurrent的啊

Use sufree

那这个是这样的问题也呃也很多啊

甚至啊

甚至啊

这个有兴趣的同学可以去看一下这个ghost race

甚至在没有use are free的时候

你的程序是没有use are free的

因为你的处理器会投机执行哎

这也是一个非常也是一个非常有趣的

也是一个非常有趣的特性啊

你假设你有同步对吧

你有lock和unlock好

你在这里有use use

然后呃那这个时候lock你你这里free

然后你可以在这里

比如判断这个这个指针是不是啊

你这有一个unlock

比如说free以后

你会有一个pointer等于等于空

你会把这个指针置为空

然后如果如果这个pointer的话

你你会use对吧

你会你会reference这个point好呃

这个程序是正确的

它它正确的实现了互斥

麻烦的是这个lock实际上是这样实现的

就是如果这个一个一个atomic的exchange

它成功了

如果他如果他成功了

他就会进入进入进入这个呃lock的region

然后否则的话如果他失败的话

如果他失败

他就要go to retry

它是这样实现的好

那从顺序执行的逻辑上

这是没问题的

因为我我我这unlock

我我这个才会retry才会终止

然后我才会执行执行后面的代码

所以我的这个这个读PTR一定是happen after

这个的一定是害怕after after这个

所以这个代码一定不会执行

这部分代码一定不会执行

但是我们的CPU他很聪明

他比你想象的聪明

有一个依附条件

在他不知道判断真假的时候

你的CPU

实际上有可能把两个条件都给执行了

但是他把两个条件都给执行

但是他会把那个预测错误的那一半给抹掉

如果CPU还有算力的话

这样他不就可以弥补这个分支预测的延

迟啊等等

反正因为我指令都已经load加载了

也执行了

但是这些指令的投机执行

会导致你可能会

比如说这部分代码会留下一些像cash的

footprint的这些side channel

一些测性到

然后你的呃可以通过比如说访问时间啊等等

去去去偷出一些pointer

因为这边有developments里面的里面的信息

所以所以如果有一条路径是投机执行的对吧

我这是concurrent

在投机执行的时候

我的锁互斥锁现在其实是不存是

是不是是不在的对吧

我的互斥锁锁是获取不到的

但是我获取不到的时候

如果我的CPU去投机执行这条路径诶

这个时候pointer就是在的

他读这个pointer

这个就是在的

然后然后这个pointer在的时候

他就会直投机再去投机

剩下的东西

如果这个有可能会

如果这个有可能会导致信息泄露的话

这就是一个可能利用的呃漏洞

反正就是非常复杂的

各种你想都呃想都想不清楚的这个呃事情对

所以我我今天这节课我讲的是并发G

虽然我讲的是并发bug

实际上我讲的是呃

harness engineering的本质

也就是软件工程的本质

软件工程的本质就是你程序员都是不能相信的

软件世界本身就是复杂的

你要尽可能的去做自动化

不仅是是是是在编码上面有一些

比如说lock一定要配对

然后尽可能的用这个RAWI啊

以及你可以实现各种各样的辅助工具

以最大的不信任揣测程序员

然后这一点会帮助AI

能够快速找到系统里面的问题

然后把系统变得啊变得更对对吧

我们理论上说任何一个程序的执行

我们都可以做一个加强版的lockstep

或者加强版的thread sitizer

我们可以把整个程序的执行

都对齐到时间轴上对吧

因为本质上程序的执行就是指令嘛

指我可以把指令的执行对齐到时间轴上

然后我的每一个函数调用对吧

我的每一次函数调用你的你的时间轴

你哪怕是有是有现成的

我都可以

我都可以这样对嘛

你们看过那个PROFETO的那些trace对啊

比如说我这调用了do some

然后我do some的时候

我肯定就是干了一件什么事对吧

然后我又调用了do some

然后我在这里面的do some里面

我在这个线程的do some里面

我有一个lock

然后我有个lock

我现在因为这个现在我block了对吧

我等了一会儿

然后我再实际上执行了do sum

然后我再release等等

实际上你是可以把一个程序完整的执行

全部都对齐到时间轴上的

也就是说你就想想回到我们说并发编程的时候

不仅是并发编程

我们完全可以把每一个程序

就想象成是一个人嘛

然后每一个人我做的每一件事情都是有动机

都是有解释的

我今天早上要吃饭

是因为我饿

我不吃饭

我能量就不够了

然后我今天来上课是为了得为了期末

期末考试不要崩

期末考试不要崩

我还想找个好工作

你做的每一件事情都是有一个自己的预期

有一个其实就是可以解释的

程序也是程序

更是因为程序是人写的

程序是物理世界里面发生的事情

在信息世界里的投影

那我我为什么不能我的程序一边在执行一边

我就在输出日志

在解释自己发生了什么

然后我一边在解释日志的时候

我这个日志是不是就变得可以审计了

我也可以让另外一个模型来来监工

你这里有没有发生

All the violation

你这个顺序排的对不对

你你有没有

因为你你想刚才我们说那个time of check to time of use

本质上是说我想要解决的是呃

不能往符号链接里面写数据

那我一边执行函数的时候

内核里面那个函数就知道

我打开了一个符号链接

那如果我外面站了一个大语言模型

我就完全可以看到这个是一个不安全的

不正常的访问

我应该把它呃拦截下来

所以我觉得我们的未来呃

程序的世界

软件工程的世界呃

都会都会发生很大的变化

我觉得我们的程序程序程序有formal semantics

就是每一个每一个呃

指令的执行都是well defined的

然后每个语句的执行也是well defined

每个函数的执行也是well defined

就是因为这种well defined和determinism

给了我们控制计算机的这种绝对控制力

但是我们也可以给这个每一个函数

一个informal semantics

让他报告他做了什么

然后我们可以把这些报告都串联起来

就可以知道这个程序到底做了什么

这样可以帮AI帮我们debug

可以帮AI帮我们审计啊

这都是未来有可能会发生的呃

比较有趣的事情